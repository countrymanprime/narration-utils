{
  "name": "narration-utils-shell",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:windows": "tauri build --config src-tauri/tauri.release.conf.json --bundles nsis",
    "build:macos": "tauri build --config src-tauri/tauri.release.conf.json --bundles dmg",
    "build:linux": "tauri build --config src-tauri/tauri.release.conf.json --bundles appimage,deb"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
