{
  "name": "narration-utils-shell",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wails dev",
    "build": "wails build -o narration-utils-shell.exe",
    "build:windows": "pnpm run build -- -platform windows/amd64 -nsis",
    "build:macos": "pnpm run build -- -platform darwin/universal",
    "build:linux": "pnpm run build -- -platform linux/amd64"
  },
  "devDependencies": {}
}
