{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. The webview loads the in-process axum-served app directly and never calls back into Tauri commands, so no extra permissions are granted beyond the framework's own defaults.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
