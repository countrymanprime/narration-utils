[
  {
    "name": "a stale GUID resolves to another item",
    "file": "narration_line_identity.lua",
    "find": "local item = items[row.guid]\n    if not item then",
    "replace": "local item = items[row.guid] or reaper.GetMediaItem(0, 0)\n    if not item then"
  },
  {
    "name": "a different existing line ID is overwritten without asking",
    "file": "narration_line_identity.lua",
    "find": "elseif current ~= '' and current ~= row.id and not overwrite then",
    "replace": "elseif false then"
  },
  {
    "name": "stamping is not idempotent",
    "file": "narration_line_identity.lua",
    "find": "if current == row.id and ext_value(item, LINE_TEXT_KEY) == row.text then",
    "replace": "if false then"
  },
  {
    "name": "stamping opens an undo block even when nothing changed",
    "file": "narration_line_identity.lua",
    "find": "if #plan.apply > 0 then\n    reaper.Undo_BeginBlock2(0)\n    for _, entry in ipairs(plan.apply) do",
    "replace": "if true then\n    reaper.Undo_BeginBlock2(0)\n    for _, entry in ipairs(plan.apply) do"
  },
  {
    "name": "the unresolved-GUID event cap is lifted",
    "file": "narration_line_identity.lua",
    "find": "local UNRESOLVED_EVENT_LIMIT = 50",
    "replace": "local UNRESOLVED_EVENT_LIMIT = 5000"
  },
  {
    "name": "an existing region is created again",
    "file": "narration_line_identity.lua",
    "find": "found = found or same_region(region, row.title, row.first, row.last)",
    "replace": "found = false"
  },
  {
    "name": "the region tolerance widens",
    "file": "narration_line_identity.lua",
    "find": "local REGION_TOLERANCE = 0.01",
    "replace": "local REGION_TOLERANCE = 1"
  },
  {
    "name": "a malformed region row is accepted",
    "file": "narration_line_identity.lua",
    "find": "if first and last and first >= 0 and last > first and title ~= '' then",
    "replace": "if first and last and title ~= '' then"
  },
  {
    "name": "the duplicate marker window widens",
    "file": "narration_bridge_core.lua",
    "find": "math.abs(marker_pos - srcpos) <= 0.15",
    "replace": "math.abs(marker_pos - srcpos) <= 5"
  },
  {
    "name": "export does not re-check for a marker the narrator added meanwhile",
    "file": "narration_compare.lua",
    "find": "          local existing_name = existing(entry.take, fields[3], srcpos)\n          if existing_name then\n            row.state = 'existing'",
    "replace": "          local existing_name = nil\n          if existing_name then\n            row.state = 'existing'"
  },
  {
    "name": "export writes an undo point even when nothing was added",
    "file": "narration_compare.lua",
    "find": "if added > 0 then\n    reaper.Undo_OnStateChange",
    "replace": "if true then\n    reaper.Undo_OnStateChange"
  },
  {
    "name": "a command with another protocol version is executed",
    "file": "narration_ui_bridge.lua",
    "find": "if command[1] ~= '1' then",
    "replace": "if false then"
  },
  {
    "name": "half-written command files are read",
    "file": "narration_bridge_core.lua",
    "find": "if name:match('%.cmd$') then",
    "replace": "if true then"
  },
  {
    "name": "close does not stop the loop",
    "file": "narration_ui_bridge.lua",
    "find": "active = false",
    "replace": "active = true"
  },
  {
    "name": "jump moves the cursor without selecting only that item",
    "file": "narration_compare.lua",
    "find": "reaper.SelectAllMediaItems(0, false)\n      reaper.SetMediaItemSelected(row.item, true)",
    "replace": "reaper.SetMediaItemSelected(row.item, true)"
  },
  {
    "name": "command files are processed in listing order instead of name order",
    "file": "narration_bridge_core.lua",
    "find": "table.sort(names)",
    "replace": ""
  },
  {
    "name": "a second feature can register a command name that already exists",
    "file": "narration_bridge_core.lua",
    "find": "if commands[name] then\n      error(",
    "replace": "if false then\n      error("
  },
  {
    "name": "an unsupported command's error loses the run it belongs to",
    "file": "narration_ui_bridge.lua",
    "find": "ctx.event('ERROR', command[3] or '', 'Unsupported workspace command')",
    "replace": "ctx.event('ERROR', '', 'Unsupported workspace command')"
  },
  {
    "name": "the command listing is not refreshed (REAPER caches it)",
    "file": "narration_bridge_core.lua",
    "find": "reaper.EnumerateFiles(directory, -1)\n  local names",
    "replace": "local names"
  },
  {
    "name": "a duplicate pickup is imported again",
    "file": "narration_pickups.lua",
    "find": "if pickup_exists(markers, row.start, body) then",
    "replace": "if false then"
  },
  {
    "name": "import opens an undo block even when nothing changed",
    "file": "narration_pickups.lua",
    "find": "if #pending > 0 then\n    reaper.Undo_BeginBlock2(0)",
    "replace": "if true then\n    reaper.Undo_BeginBlock2(0)"
  },
  {
    "name": "the pickup duplicate tolerance widens",
    "file": "narration_pickups.lua",
    "find": "local PICKUP_TOLERANCE = 0.15",
    "replace": "local PICKUP_TOLERANCE = 5"
  },
  {
    "name": "a malformed pickup row is accepted",
    "file": "narration_pickups.lua",
    "find": "if start and start == start and start >= 0 and start < math.huge and note ~= '' then",
    "replace": "if start then"
  },
  {
    "name": "export reports a resolved pickup as still open",
    "file": "narration_pickups.lua",
    "find": "for _, marker in ipairs(project_markers()) do\n    if marker_kind(marker.name) == 'PICKUP' then\n      local tag, note = split_pickup_body(marker_body(marker.name))",
    "replace": "for _, marker in ipairs(project_markers()) do\n    if marker_kind(marker.name) ~= 'PICKUP_DONE' then\n      local tag, note = split_pickup_body(marker_body(marker.name))"
  },
  {
    "name": "count_pickups counts a resolved pickup as remaining",
    "file": "narration_pickups.lua",
    "find": "if kind == 'PICKUP' then\n      remaining, total = remaining + 1, total + 1",
    "replace": "if kind == 'PICKUP' or kind == 'PICKUP_DONE' then\n      remaining, total = remaining + 1, total + 1"
  },
  {
    "name": "next_pickup ignores the edit cursor",
    "file": "narration_pickups.lua",
    "find": "for _, marker in ipairs(pickups) do\n    if marker.pos > cursor then",
    "replace": "for _, marker in ipairs(pickups) do\n    if true then"
  },
  {
    "name": "next_pickup can land on a resolved pickup",
    "file": "narration_pickups.lua",
    "find": "for _, marker in ipairs(project_markers()) do\n    if marker_kind(marker.name) == 'PICKUP' then\n      pickups[#pickups + 1] = marker",
    "replace": "for _, marker in ipairs(project_markers()) do\n    if marker_kind(marker.name) ~= 'PICKUP_DONE' then\n      pickups[#pickups + 1] = marker"
  },
  {
    "name": "resolve_pickup ignores the position tolerance",
    "file": "narration_pickups.lua",
    "find": "if gap <= PICKUP_TOLERANCE and (not best_gap or gap < best_gap) then",
    "replace": "if (not best_gap or gap < best_gap) then"
  },
  {
    "name": "resolve_pickup can resolve an already-resolved pickup",
    "file": "narration_pickups.lua",
    "find": "for _, marker in ipairs(project_markers()) do\n    if marker_kind(marker.name) == 'PICKUP' then\n      local gap = math.abs(marker.pos - target)",
    "replace": "for _, marker in ipairs(project_markers()) do\n    if marker_kind(marker.name) ~= '' then\n      local gap = math.abs(marker.pos - target)"
  },
  {
    "name": "configure_chapter_render accepts an empty output folder",
    "file": "narration_render.lua",
    "find": "if output_folder == '' then",
    "replace": "if false then"
  },
  {
    "name": "configure_chapter_render runs without the render-config API",
    "file": "narration_render.lua",
    "find": "if not reaper.APIExists('GetSetProjectInfo_String') or not reaper.APIExists('GetSetProjectInfo') then",
    "replace": "if false then"
  },
  {
    "name": "project_state runs without the change-count API",
    "file": "narration_project_state.lua",
    "find": "if not reaper.APIExists('GetProjectStateChangeCount') then",
    "replace": "if false then"
  },
  {
    "name": "project_state reports a hardcoded 0 instead of the real change count",
    "file": "narration_project_state.lua",
    "find": "local count = reaper.GetProjectStateChangeCount(0)",
    "replace": "local count = 0"
  },
  {
    "name": "create_take's stale target item GUID resolves to another item",
    "file": "narration_take_review.lua",
    "find": "local target_item = items[normalized_target]",
    "replace": "local target_item = items[normalized_target] or reaper.GetMediaItem(0, 0)"
  },
  {
    "name": "create_take's stale candidate item GUID check is skipped",
    "file": "narration_take_review.lua",
    "find": "if normalized_candidate ~= '' and not items[normalized_candidate] then",
    "replace": "if false then"
  },
  {
    "name": "create_take does not check the candidate source file exists",
    "file": "narration_take_review.lua",
    "find": "if not core.file_exists(payload.source_file) then",
    "replace": "if false then"
  },
  {
    "name": "a stale navigation GUID resolves to another item",
    "file": "narration_navigation.lua",
    "find": "local item = wanted_item ~= '' and find_item(wanted_item) or nil",
    "replace": "local item = wanted_item ~= '' and find_item(wanted_item) or reaper.GetMediaItem(0, 0)"
  },
  {
    "name": "a take GUID no longer on the item falls back to the active take",
    "file": "narration_navigation.lua",
    "find": "  local take = find_take(item, wanted_take)\n  if not take then\n    return nil, nil, take_text, 'take'",
    "replace": "  local take = find_take(item, wanted_take) or reaper.GetActiveTake(item)\n  if not take then\n    return nil, nil, take_text, 'take'"
  },
  {
    "name": "navigate_item goes to a spot the item no longer covers",
    "file": "narration_navigation.lua",
    "find": "    target = project_time(item, take, source_time)\n    if target < first - EDGE_TOLERANCE or target > last + EDGE_TOLERANCE then",
    "replace": "    target = project_time(item, take, source_time)\n    if false then"
  },
  {
    "name": "a loop window outside the item is played anyway",
    "file": "narration_navigation.lua",
    "find": "if window_end <= window_start then",
    "replace": "if false then"
  },
  {
    "name": "navigation does not refuse while REAPER is recording",
    "file": "narration_navigation.lua",
    "find": "local function recording()\n  return math.floor(reaper.GetPlayState() / 4) % 2 == 1",
    "replace": "local function recording()\n  return false"
  },
  {
    "name": "a second loop overwrites what the narrator had before the first",
    "file": "narration_navigation.lua",
    "find": "  if not state.loop then\n    state.loop = { saved = capture() }",
    "replace": "  if true then\n    state.loop = { saved = capture() }"
  },
  {
    "name": "stop_loop overwrites a time selection the narrator changed during the loop",
    "file": "narration_navigation.lua",
    "find": "if same_range({ first, last }, loop.window[1], loop.window[2]) then",
    "replace": "if true then"
  },
  {
    "name": "stop_loop does not restore repeat",
    "file": "narration_navigation.lua",
    "find": "    reaper.GetSetRepeat(loop.saved.repeat_on)\n",
    "replace": "\n"
  },
  {
    "name": "stop_loop restores twice",
    "file": "narration_navigation.lua",
    "find": "  state.loop = nil\n  if playing() and not recording() then",
    "replace": "  if playing() and not recording() then"
  },
  {
    "name": "stop_loop stops playback it did not start",
    "file": "narration_navigation.lua",
    "find": "    event(session_dir, 'LOOP_STOPPED', run_id, 0, 0)\n    return",
    "replace": "    reaper.OnStopButton()\n    event(session_dir, 'LOOP_STOPPED', run_id, 0, 0)\n    return"
  },
  {
    "name": "the script ending with a loop running does not restore",
    "file": "narration_navigation.lua",
    "find": "        if state.loop then\n          restore(state.loop)",
    "replace": "        if false then\n          restore(state.loop)"
  },
  {
    "name": "compare markers carry the item's GUID read at inspect time instead of at prepare time",
    "file": "narration_compare.lua",
    "find": "          entry.item_guid,\n",
    "replace": "          select(2, reaper.GetSetMediaItemInfo_String(entry.item, 'GUID', '', false)),\n"
  },
  {
    "name": "the approved marker is added outside an undo block",
    "file": "narration_navigation.lua",
    "find": "  reaper.Undo_BeginBlock2(0)\n  reaper.SetTakeMarker(take, -1, name, source_time, color ~= '' and core.color(color) or 0)\n  reaper.Undo_EndBlock2(0, UNDO_ADD_MARKER, -1)",
    "replace": "  reaper.SetTakeMarker(take, -1, name, source_time, color ~= '' and core.color(color) or 0)"
  },
  {
    "name": "the approved marker doubles a marker the take already has",
    "file": "narration_navigation.lua",
    "find": "local existing_name = core.existing_take_marker(take, core.marker_kind(name), source_time)",
    "replace": "local existing_name = nil"
  },
  {
    "name": "the approved marker is added while REAPER is recording",
    "file": "narration_navigation.lua",
    "find": "'The marker has no name.')\n    return\n  end\n  if recording() then",
    "replace": "'The marker has no name.')\n    return\n  end\n  if false then"
  },
  {
    "name": "the approved marker is added at a spot the item no longer covers",
    "file": "narration_navigation.lua",
    "find": "  local target = project_time(item, take, source_time)\n  if target < first - EDGE_TOLERANCE",
    "replace": "  local target = first\n  if target < first - EDGE_TOLERANCE"
  },
  {
    "name": "launch_cleanup_tool launches a tool that is not on the allow-list",
    "file": "narration_cleanup.lua",
    "find": "local tool = TOOLS[key]\n  if not tool then",
    "replace": "local tool = TOOLS[key] or { label = key, matches = function() return true end, missing = '' }\n  if false then"
  },
  {
    "name": "launch_cleanup_tool runs without the action-list API",
    "file": "narration_cleanup.lua",
    "find": "if not reaper.APIExists('kbd_enumerateActions') or not reaper.APIExists('SectionFromUniqueID') then",
    "replace": "if false then"
  },
  {
    "name": "launch_cleanup_tool launches with no item selected",
    "file": "narration_cleanup.lua",
    "find": "if reaper.CountSelectedMediaItems(0) == 0 then",
    "replace": "if false then"
  },
  {
    "name": "launch_cleanup_tool guesses the first of several matching actions",
    "file": "narration_cleanup.lua",
    "find": "if #found > 1 then",
    "replace": "if false then"
  },
  {
    "name": "a script or custom action named like a native tool counts as the native tool",
    "file": "narration_cleanup.lua",
    "find": "if category == 'script' or category == 'custom' then",
    "replace": "if false then"
  },
  {
    "name": "pick_retake_lane plays a lane on a track that is not in fixed-lane mode",
    "file": "narration_retake_lanes.lua",
    "find": "if reaper.GetMediaTrackInfo_Value(track, 'I_FREEMODE') ~= FIXED_LANE_MODE then",
    "replace": "if false then"
  },
  {
    "name": "pick_retake_lane turns lanes on for a track that has none, instead of refusing it",
    "file": "narration_retake_lanes.lua",
    "find": "  reaper.Undo_BeginBlock2(0)\n  reaper.SetMediaTrackInfo_Value(track, 'C_LANEPLAYS:' .. lane, PLAYS_EXCLUSIVELY)",
    "replace": "  reaper.Undo_BeginBlock2(0)\n  reaper.SetMediaTrackInfo_Value(track, 'I_FREEMODE', FIXED_LANE_MODE)\n  reaper.SetMediaTrackInfo_Value(track, 'C_LANEPLAYS:' .. lane, PLAYS_EXCLUSIVELY)"
  },
  {
    "name": "pick_retake_lane plays an item that no longer carries the line",
    "file": "narration_retake_lanes.lua",
    "find": "if stamped ~= line_id then",
    "replace": "if false then"
  },
  {
    "name": "pick_retake_lane adds the lane alongside the others instead of playing it alone",
    "file": "narration_retake_lanes.lua",
    "find": "local PLAYS_EXCLUSIVELY = 1",
    "replace": "local PLAYS_EXCLUSIVELY = 2"
  },
  {
    "name": "pick_retake_lane plays the lane through the read-only item-level C_LANEPLAYS",
    "file": "narration_retake_lanes.lua",
    "find": "reaper.SetMediaTrackInfo_Value(track, 'C_LANEPLAYS:' .. lane, PLAYS_EXCLUSIVELY)",
    "replace": "reaper.SetMediaItemInfo_Value(item, 'C_LANEPLAYS', PLAYS_EXCLUSIVELY)"
  },
  {
    "name": "pick_retake_lane changes the play state outside an undo block",
    "file": "narration_retake_lanes.lua",
    "find": "  reaper.Undo_BeginBlock2(0)\n  reaper.SetMediaTrackInfo_Value(track, 'C_LANEPLAYS:' .. lane, PLAYS_EXCLUSIVELY)\n  reaper.Undo_EndBlock2(0, 'Narration Utils: play retake lane ' .. (lane + 1), -1)",
    "replace": "  reaper.SetMediaTrackInfo_Value(track, 'C_LANEPLAYS:' .. lane, PLAYS_EXCLUSIVELY)"
  },
  {
    "name": "pick_retake_lane runs without the fixed-lane API",
    "file": "narration_retake_lanes.lua",
    "find": "if not has_lane_api() then",
    "replace": "if false then"
  }
]
