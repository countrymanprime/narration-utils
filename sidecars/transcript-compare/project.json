{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "transcript-compare",
  "projectType": "application",
  "tags": ["toolchain:python"],
  "implicitDependencies": ["narration-common", "config"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node scripts/quality.mjs python -m ruff format --check sidecars/transcript-compare",
          "node scripts/quality.mjs python -m ruff check sidecars/transcript-compare"
        ],
        "parallel": false
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node scripts/ci/coverage-gate.mjs pytest sidecars/transcript-compare sidecars/transcript-compare/tests"
      }
    }
  }
}
