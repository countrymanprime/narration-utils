# Single, repo-wide Python dependency set for all first-party tools
# (Manuscript Guide, Transcript Compare, and their shared helpers). The
# checkout bootstrap installs the fully pinned requirements.lock into one venv.
python-docx==1.2.0
pypdf==6.1.3
spacy>=3.7
pronouncing>=0.2
phonemizer>=3.3
piper-tts==1.8.0
# Optional: install a local Piper executable/model separately for WAV previews.
faster-whisper==1.2.1
ctranslate2==4.8.2
av==18.1.0
numpy==2.5.3
fastapi==0.121.2
uvicorn==0.38.0
httpx==0.28.1
