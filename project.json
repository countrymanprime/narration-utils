{
  "name": "narration-utils",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": ".",
  "targets": {
    "build": {
      "command": "npm --prefix shared/ui run build && npm --prefix shell run build"
    },
    "test": {
      "command": "npm --prefix shared/ui test"
    },
    "lint": {
      "command": "npm --prefix shared/ui run lint:ci"
    }
  }
}
