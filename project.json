{
  "name": "narration-utils",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": ".",
  "tags": ["root"],
  "$comment": "The workspace root and the nx release project. Every other project depends on it, so a change to a root-owned file (root config, docs, .github) affects them all. Per-project lint, format, test and build targets live in each project.json."
}
