{
  "name": "narration-utils",
  "$schema": "./node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": ".",
  "targets": {
    "build": {
      "command": "pnpm --dir shared/ui run build && pnpm --dir shell run build"
    },
    "test": {
      "command": "pnpm --dir shared/ui test"
    },
    "lint": {
      "command": "pnpm --dir shared/ui run lint:ci"
    }
  }
}
