# Implementation plan for all PRDs

**Status:** in execution. This file is transient: it is deleted in the PR that completes the last stack, like the PRDs it sequences ([ADR 0028](../adr/0028-planned-work-is-specified-as-prds-and-deleted-when-built.md)).

**Source:** owner instruction of 2026-09-20: turn every PRD into a phased plan, answer the open questions first, use a dev-team style review of the cross-PRD conflicts, deliver each PRD as stacked pull requests without pausing, open an ADR (Status: Proposed) for anything that needs the owner and keep going, and replace each PRD with steady-state documentation when its work is done.

It covers the 37 PRDs in this folder. It does not replace them: each PRD still holds its problem, evidence and phase detail. This plan holds what no single PRD can: the owner's answers to the open questions, the order the PRDs run in, and the rules every stack follows.

## 1. Owner decisions (2026-09-20)

These override the PRD text they contradict. The first pull request of each stack reconciles its PRD (ticks the answered questions, adds Decisions Log rows, corrects stale paragraphs) before any code.

| # | Decision | Effect |
| --- | --- | --- |
| D1 | UI library is **Base UI**, wrapped in our own Tailwind primitives so every control is styled one way. | The foundation PRD was the spec and is delivered (stack S10a, [ADR 0047](../adr/0047-the-ui-primitives-wrap-base-ui-and-app-code-never-imports-it.md) to ADR 0052); its PRD file is deleted. The primitives PRD that followed it is delivered as stack S10b (ADR 0053 to ADR 0056) and deleted. `Switch` was built by S10a and waits for the Settings boolean kind. |
| D2 | Pull the **Lua test harness** (Lua 5.4 in CI, fake `reaper` table) and a behavior-preserving **command registry** forward, before any new Lua command. | [reaper-automation-follow-through](reaper-automation-follow-through.prd.md) phases 3 and 4 are unconditional and first. "Lua has no tests, manual checklist is the merge gate" is stale in every PRD. Manual REAPER sign-off remains only for REAPER API semantics. |
| D3 | **All six REAPER spikes are approved** on `C:\Users\Count\Documents\REAPER Media\Projects\Challenges\Challenges_001.rpp`. **Never delete or modify anything in that folder**: work on copies in a temp directory with an isolated `-cfgfile`. S1, S3 and S4 need audio hardware and the owner present: mark them pending and continue. | Spikes S0, S5, S7 and the take-mechanics spike can run unattended. |
| D4 | Overlap owners: **dialog modality** belongs to the Base UI foundation (dialog PRD keeps the defect scope); **release-readiness Phase 1** owns the shared install-poll hook; the two teleprompter PRDs share one `MicrophoneField` seam. | Removes three duplicate builds. |
| D5 | **Palette text ramp is option B** (two text levels plus a 3:1 non-text token, retire `--text-faint` as text, triage about 73 usages). Highlight and badge text use the colour-mix option A. | The palette PRD's Phase 2 grew from a token edit to a triage plus migration (delivered as slices 2a to 2e; see [colour and contrast](../design/colour-and-contrast.md)). |
| D6 | Info icons become real buttons; tooltips meet **WCAG 1.4.13 formally** (hoverable, persistent, Escape-dismissible). Tooltip delay stays 1000 ms (assumption, from L7). | Info-icon text lives in a Base UI Popover; hint tooltips stay Tooltips whose trigger carries the same text. |
| D7 | **First stable is Windows-only and unsigned.** macOS and Linux stay preview assets. Signing is the owner's call: no agent adds signing steps. | [release-readiness](release-readiness-provisioning-and-docs-site.prd.md) Phase 15 is deferred; RC notes say "unsigned". |
| D8 | OS **notifications are on by default** (unfocused, job of about 10 s or more). **Build Story Bible after import is on by default.** | Contradicts the briefs PRD B1 and its "opt-in" wording. Default-on makes host-side completion events (the interaction feedback audit's Phase 5, delivered by stack S14: [ADR 0076](../adr/0076-a-host-job-ends-with-one-job-ended-event-the-app-announces-it-and-the-story-bible-rebuild-may-continue-in-the-background.md)) and the dialog PRD's Q4 (blocking build) load-bearing: reconcile them. |
| D9 | **Chapter stage recommendations stay unscheduled** on the roadmap. Still implemented, last. | No edits to `docs/roadmap.md`, the roadmap JSON or GitHub milestones from those five PRDs. Drop SR Q14 and any "propose a milestone" step. |
| D10 | The app may **auto-start the launcher script** when it launches REAPER, if the project-workspace Phase 6 spike proves it works. | Ship behind a Settings toggle; record in an ADR. |
| D11 | Repository rulesets vs docs: **the docs change to match the live settings** (code-owner review required, no required status checks). | The supply-chain PRD's `CI / gate` design and Q8, Q9 are moot; its Phase 6 becomes a docs edit plus an owner checklist. |
| D12 | Red **danger confirms**: Clear derived project data, Remove local preview voice, Remove local Whisper model, Delete entry, Merge & delete source, Replace and reset. | Dialog PRD Q8 answered. |
| D13 | Story Bible pronunciation **generate/replace lives in edit mode only**, blocked for locked entries. | [Generating and replacing a pronunciation](../guides/using-the-app/story-bible.md#generating-and-replacing-a-pronunciation) (delivered; PRD deleted). |
| D14 | **The version lives inside the app, not in asset file names.** The app knows its own version, checks GitHub releases and downloads and replaces itself. The program is renamed `narration-utils`. Asset names stay unversioned. | Drops release-artifact-naming (PRD deleted, delivered) Phase 2; keeps Phase 1. A **new PRD** (`in-app-update`) is written and delivered. Ambiguous wording: the reading is recorded in an open ADR for review. |
| D15 | **Repo layout runs first as one atomic PR** (retires `shared/`, `shell/` to `apps/desktop`, per-project Nx). A2-A8 take the PRD's recommendations: `apps/desktop`, keep the Go module path, top-level `sidecars/`, tests in `<project>/tests/`, Nx per project as a follow-up phase in the same stack. Local `.claude/skills` are untracked: update them locally after the move. | Every path in every other PRD changes; the layout stack ships a codemap. |
| D16 | Runtime validation at the data boundaries uses **Zod 4 behind `parseWire`**. | Delivered as stack S08 ([ADR 0069](../adr/0069-payloads-are-validated-with-zod-behind-parsewire-and-a-wrong-shape-fails-loudly.md), [wire contracts](../architecture/wire-contracts.md)); its PRD is deleted. Every new wire contract gets a schema and a golden payload; mocks must pass it. |
| D17 | **The project relicenses from MIT to AGPL-3.0-or-later**, so it stays free for narrators. Bundling GPL Piper, phonemizer and eSpeak NG is fine; there is no separate-asset split. Model and voice licences are still checked per artifact. | New stack `relicense`. The docs security and hygiene PRD (delivered by S17, deleted) had Q12 collapse; Q13 and Q14 stay (add GPL/AGPL/LGPL to the allow-list). Any new dependency (Moonshine, an ID3 or MP3 library, Praat, Resemblyzer) needs an AGPL-compatibility check. |
| D18 | Coverage policy is a **ratchet on logic directories** with an 80% floor for any new logic directory; UI, glue and sidecar-launch code are exempt. A logged deviation from the global 80% rule. | Replaces every "at least 80% on new Go" gate. |
| D19 | The first **real audio corpus is the `Challenges_001` project's media plus synthetic generators**, read-only. | Analyzer thresholds are "validated" only for the conditions that corpus covers (probably one narrator and room); say so in evidence and ADRs. Escalate with an open ADR where it cannot support a claim (for example separating characters). |
| D20 | **Cadence: stacked phases per PRD, no pausing.** Each phase is a branch and a PR based on the previous one; the owner merges in order. | See section 3. Never merge, never enable auto-merge. |
| D21 | **When a PRD is done, replace it with steady-state documentation** (README lifecycle step 3). | The last PR of each stack writes the docs, ADRs and deletes the PRD. |
| D22 | Every open question not listed here **adopts the PRD's stated recommendation**. Anything that needs the owner and has no recommendation becomes an **open ADR** (Status: Proposed) and work continues on the recommended path. | The PR that creates an ADR lists it under "New ADRs for review". |
| D23 | **Audacity integration runs as a parallel stack now**, reversing the roadmap's "Audacity adapters after the shared contract and REAPER workflow are proven" deferral. [`audacity-integration.prd.md`](audacity-integration.prd.md) is written and added to the train as stack S25. | `docs/roadmap.md`'s deferred-work list and `config/roadmap.json`'s `deferred` array drop "Audacity adapters" (done in this PR); `reaper-automation-follow-through.prd.md`'s "Won't Be Building"/Non-Users mentions of Audacity are stale wording, corrected by the new PRD's own reconciliation note rather than edited in place (that PRD's Phase 0 already ran). S25 is spike-gated on its own unverified transport (`mod-script-pipe`), not on stack S22 finishing; it touches no file S22 owns except by convention (`scripts/release/verify-installable.mjs`, only if Question 3 in the new PRD resolves to needing a release artifact). |

Owner-only actions no agent can take (collect them, do not block on them): the GitHub "Require actions pinned to a SHA" policy (supply-chain Q4), enabling GitHub Pages, immutable releases (deferred), code signing certificates and secrets, and merging.

## 2. Inputs only the owner can give

Nothing below blocks the train; each phase starts with the path that needs no input, and the gated phase is marked pending.

| Input | Where it matters | Default until given |
| --- | --- | --- |
| Exact toast text and entry for the TTS preview failure (T1, T2) | the Story Bible preview PRD (deleted; see [the Story Bible preview](../architecture/story-bible-preview.md)) | Phase 1 diagnosed against every candidate cause on a dev build; never supplied. |
| `vocabulary_candidates` contents and toast text (V1) | [proofing-vocabulary-hints](proofing-vocabulary-hints.prd.md) | Go always derives candidates from entities (V2 option a). |
| A real manuscript with a Word TOC field (S6); 3 or more manuscripts with wrong title/subtitle splits (I1) | import-structure Phase 4; briefs Phase 5 | TOC work uses a generated fixture; the subtitle override stays gated. |
| A real proofer pickup export (Pozotron or similar) | RF Phase 9 | Generic CSV only. |
| Owner present at the audio interface | RF spikes S1, S3, S4; teleprompter A/B (TE-8); TM punch (TM-12) | Marked pending. |
| Multi-character reference audio | character-continuity Phase 1 | Trial covers narration vs dialogue on the `Challenges_001` media only; open ADR if it cannot separate characters. |

## 3. Execution protocol (every stack)

1. **Order.** Stacks run in the order of section 4. Each stack's first PR is based on the tip of the previous stack; the very first is based on `main`. The owner merges in order, so a linear train has no sibling conflicts. Re-read `docs/adr/` and `hostAPIVersion` at the base tip: ADR numbers and API versions are allocated sequentially along the train.
2. **Issue first.** One tracking issue per PRD (`gh issue create`, Feature request or Bug report form conventions, `needs-triage`, `area:*` labels; see [github-workflow](../operations/github-workflow.md)). PRs say `Part of #<n>`, and only a stack's last PR says `Closes #<n>`.
3. **Reconcile the PRD first.** Phase 0 of a stack (docs only, may be folded into phase 1's PR) applies section 1 to the PRD: tick answered questions, add Decisions Log rows, fix stale paths and contradicted paragraphs. Do not silently rewrite scope.
4. **One PR per phase**, branch `<type>/<prd-slug>-p<N>-<slug>`, PR title a Conventional Commit, description = the phase's goal, what changed, verification evidence, and `Base: <previous branch>`. Set the phase's `Status` to `complete` in the PRD table in the same PR and link the plan. End commit messages and PR bodies with the attribution lines the harness gives.
5. **Plan, impact scan, TDD, gate, review** per [CLAUDE.md](../../CLAUDE.md): `change-impact-scan` before touching shared code, tests first, the full gate (`pnpm check`, not `check:fast`) plus the Playwright visual suite when the UI package changes, `design-spec-guard` for primitives and `styles.css`, the code-reviewer agent on the diff, `feature-cleanup` at the end. Look at the screenshots for visual fixes at every viewport.
6. **Lua.** After the harness lands, Lua changes need harness tests. Real-REAPER checks run scripted against a copy of `Challenges_001.rpp` in a temp directory with an isolated `-cfgfile`; anything needing hardware or the owner is recorded in the PR as pending, not skipped silently.
7. **ADRs.** A real decision gets an ADR through the `adr-author` skill after the code lands. If a decision needs the owner and cannot be settled from D1-D22, write the ADR with `Status: Proposed`, follow the recommended path, and add a **"New ADRs for review"** section to the PR body naming each one. Never edit an Accepted ADR: supersede it.
8. **Last PR of a stack** writes the steady-state docs (`docs/architecture/`, `docs/utilities/`, `docs/guides/`, ADRs), deletes the PRD, updates the README index and this plan's status table, and closes the issue. Delete a PRD only when every phase is `complete` or explicitly deferred with an issue filed.
9. **Blocked.** If a gate cannot go green after a real attempt, open the PR as a draft with the failure output, mark the row `blocked` here, and continue with any phase that does not depend on it. Never weaken a test, skip a gate or add an escape hatch (`sameAs`, `undriven`, ratchet entries) without a reason written in the PR.
10. **Serialization points** ([README](README.md#cross-prd-sequencing)) hold: `hostAPIVersion` bumps three files and regenerates `Host.{js,d.ts}`; new bindings use `h.services()` (delivered by S03); one nav item per PR; primitives before the features that use them.
11. **Honest reporting.** A phase is `complete` only when its acceptance evidence exists. Report failures, skips and pending manual steps in the PR.

## 4. The train

Order is dependency first, then value. `S` = stack. A stack is a PRD delivered as stacked PRs; "tail" phases wait on a later stack and run when it lands.

| Stack | PRD (phases) | Notes |
| --- | --- | --- |
| S00 | This plan and the decisions register | Docs only, based on `main`. |
| S01 | `relicense` (new, one PR) | AGPL-3.0-or-later: `LICENSE`, package metadata, README, CONTRIBUTING (DCO), notices note, ADR. Small and independent, so it goes before the big move. |
| S02 | repo-layout-by-role (1-5; delivered, see [ADR 0040](../adr/0040-the-repository-is-laid-out-by-role-and-each-project-is-an-nx-project.md)) | One atomic move PR (phases 1-3), then Nx per project, then steady state. `wails build` and the launcher must be checked. |
| S03 | host-binding-data-race (1-4; delivered, see [host-binding-concurrency](../architecture/host-binding-concurrency.md) and [ADR 0041](../adr/0041-host-bindings-read-project-services-through-one-snapshot-accessor.md)) | Accessor first; later PRDs build on it. |
| S04 | the Story Bible preview TTS PRD P1 (delivered; P2 and P3 by S16; the PRD is deleted, see [the Story Bible preview](../architecture/story-bible-preview.md)), [proofing-vocabulary-hints](proofing-vocabulary-hints.prd.md) P1 | The small correctness fixes the README wants before layout changes. |
| S05 | verification-and-code-health-tooling (1-8, 10, 11; delivered, then phases 9 and 12 in S13; PRD deleted, see [Verification and code-health tooling](../operations/verification-tooling.md)) | Coverage ratchet (D18) early so later stacks follow it. |
| S06 | release-supply-chain-hardening (1-5b, 6 as docs, 8; delivered; phases 7 and 9 deferred to [#186](https://github.com/countrymanprime/narration-utils/issues/186) and [#187](https://github.com/countrymanprime/narration-utils/issues/187); PRD deleted, see [CI and releases](../operations/ci-and-releases.md#workflow-security)) | Pins every workflow action: land alone. 7 and 9 deferred or Could. |
| S07 | test-flakiness-and-visual-suite-stability (1-5; delivered, then phase 6 in S13; phase 7 deferred to [#153](https://github.com/countrymanprime/narration-utils/issues/153); PRD deleted, see [CI and releases](../operations/ci-and-releases.md#keeping-a-red-run-meaningful)) | Delivered before S06 (reordered: the flaky `ProjectPicker` focus test failed about 4 in 10 full-gate runs and slowed every later stack). |
| S08 | boundary-schema-validation (1-7; delivered, PRD deleted, see [wire contracts](../architecture/wire-contracts.md) and [ADR 0069](../adr/0069-payloads-are-validated-with-zod-behind-parsewire-and-a-wrong-shape-fails-loudly.md)) | Zod 4; every later contract uses it. |
| S09 | [reaper-automation-follow-through](reaper-automation-follow-through.prd.md) part 1: phases 1-5 | Harness, registry, fan-out, S0 spike and the fixture pack the ledger, matcher and take PRDs need. Part 1 is delivered (partial, see section 5); the rest of the PRD is S22. |
| S10 | base-ui-primitive-foundation (1-6; delivered as S10a, PRD deleted, see [ADR 0047](../adr/0047-the-ui-primitives-wrap-base-ui-and-app-code-never-imports-it.md)), then ui-primitives-and-headless-library (1, 4, 5; delivered as S10b, PRD deleted, see [ADR 0053](../adr/0053-icon-buttons-text-fields-and-selects-wrap-the-native-controls.md) to [ADR 0056](../adr/0056-a-presentational-table-primitive-replaces-the-dtable-class-and-rows-take-the-keyboard.md)) | |
| S11 | dialog-modality-and-workdialog-a11y (3-5; delivered, PRD deleted, see [ADR 0057](../adr/0057-a-running-job-that-cannot-be-cancelled-keeps-its-dialog-blocking-and-says-so.md)), component-a11y-meter-tooltip-field-heading-panel (3, 4; delivered, PRD deleted, see [ADR 0058](../adr/0058-heading-has-a-level-and-panel-names-its-region-with-a-level-2-title.md)) | What remained after the foundation. |
| S12 | palette-contrast-wcag-aa (1-6; delivered as S12a, PRD deleted), settings-mobile-layout (1-3; delivered as S12b, PRD deleted, see [ADR 0060](../adr/0060-the-visual-suite-fails-a-collapsed-control-and-a-row-may-declare-one-narrow-on-purpose.md) and [ADR 0061](../adr/0061-settings-states-are-also-captured-at-a-390px-reflow-width.md)) | Palette option B. Settings needs a boolean kind for D8 (delivered in S12b). |
| S13 | tail: verification 9 and 12; test-flakiness 6 (delivered, both PRDs deleted; see [ADR 0062](../adr/0062-ui-import-rules-are-a-dependency-cruiser-config-and-a-mark-scan-that-name-their-adr.md), [ADR 0064](../adr/0064-the-visual-suite-runs-axe-on-every-app-state-and-a-violation-fails-unless-it-is-declared-debt.md), [ADR 0065](../adr/0065-aria-snapshots-pin-the-role-trees-of-the-dialogs-the-slide-over-and-the-navigation.md)) | After primitives and palette. |
| S14 | interaction-feedback-audit (1-7; delivered, PRD deleted, see [interaction feedback](../architecture/interaction-feedback.md), [ADR 0075](../adr/0075-every-action-that-leaves-the-interface-acknowledges-within-100-ms-cannot-be-fired-twice-and-tells-the-narrator-when-it-ends.md) and [ADR 0076](../adr/0076-a-host-job-ends-with-one-job-ended-event-the-app-announces-it-and-the-story-bible-rebuild-may-continue-in-the-background.md)) | Host completion events before default-on build-after-import ships. |
| S15 | release-artifact-naming P1 plus version in the binary; the new in-app-update PRD, phases 0 to 5 (delivered, both PRDs deleted, see [in-app update](../architecture/in-app-update.md); phase 6, attestation verification in the app, deferred to [#199](https://github.com/countrymanprime/narration-utils/issues/199)) | D14. Open ADR 0072 for the trust model and update channel. |
| S16 | [release-readiness-provisioning-and-docs-site](release-readiness-provisioning-and-docs-site.prd.md) (1-14 delivered, 9, 11 and 14 partial on owner steps; 15 deferred, #230; 16 owner-run, #231) | Install job contract first; owns the install-poll hook (D4). |
| S17 | docs-security-and-hygiene (1-10 delivered, 11 skipped and filed as [#251](https://github.com/countrymanprime/narration-utils/issues/251); PRD deleted, see the [threat model](../architecture/threat-model.md), [model provenance](../architecture/model-provenance.md), [CI and releases](../operations/ci-and-releases.md#the-docs-link-check), [ADR 0084](../adr/0084-repository-links-are-checked-offline-on-every-pull-request-and-diagrams-are-parsed-and-drawn-without-a-third-party.md) and [ADR 0085](../adr/0085-the-third-party-notices-are-generated-from-the-release-and-ship-as-their-own-asset-and-not-inside-the-update-zip.md)) | After the layout so diagrams name final paths. |
| S18 | import-review-redesign (1-3; delivered, PRD deleted, see [the import review](../architecture/import-review.md) and [ADR 0086](../adr/0086-the-import-review-is-grouped-by-what-each-section-will-be-and-reports-repairs-instead-of-a-log.md)) | Extracts the review body from `Home.tsx` before other `Home.tsx` PRDs. |
| S19a | story-bible-entries-and-actions (delivered, PRD deleted, see [Manuscript Guide](../utilities/manuscript-guide.md), [Story Bible](../guides/using-the-app/story-bible.md) and [ADR 0087](../adr/0087-story-bible-header-actions-are-mode-based-and-lock-cannot-happen-mid-edit.md)) | Runs before S19c (import-structure needs the properties schema) and independently of S19b/S19d. |
| S19b | manuscript-reader-search-and-controls (delivered, PRD deleted; see [the manuscript guide](../guides/using-the-app/manuscript.md)) | Its Phase 5 ran before S19c's structure Phase 2. |
| S19c | [import-structure-toc-and-characters](import-structure-toc-and-characters.prd.md) | Needs S19a's properties schema (`create --properties`) for its Phase 3, and S19b's reader Phase 5 before its own Phase 2. |
| S19d | [story-bible-and-import-ux-briefs](story-bible-and-import-ux-briefs.prd.md) | Its Phases 9-11 (provider, pronunciation dictionary) coordinate with S19a's pronunciation controls; keep each PRD to its own scope. |
| S20 | [project-workspace-and-daw-link](project-workspace-and-daw-link.prd.md) (1-8), [audiobook-credits-templates](audiobook-credits-templates.prd.md) (1-5) | The spike (P6) decides D10. |
| S21 | [teleprompter-engines-and-input-devices](teleprompter-engines-and-input-devices.prd.md), [teleprompter-manuscript-integration](teleprompter-manuscript-integration.prd.md) | Shared `MicrophoneField`; final phase retires the standalone page alone. |
| S22 | [review-dashboard-and-findings-adoption](review-dashboard-and-findings-adoption.prd.md), [diagnostics-delivery-and-cleanup-tools](diagnostics-delivery-and-cleanup-tools.prd.md), rest of [reaper-automation-follow-through](reaper-automation-follow-through.prd.md) | Findings store first; one nav item per PR. |
| S23 | [analysis-evidence-ledger](analysis-evidence-ledger.prd.md) (EL), [take-review-pickups-duplicates-take-intelligence](take-review-pickups-duplicates-take-intelligence.prd.md), [character-continuity-review](character-continuity-review.prd.md) | EL parser superset before every other `parseItem` change. |
| S24 | [editing-readiness-analysis](editing-readiness-analysis.prd.md), [recording-coverage-analysis](recording-coverage-analysis.prd.md), [proofing-readiness-signals](proofing-readiness-signals.prd.md), [chapter-stage-recommendations](chapter-stage-recommendations.prd.md), [proofing-preview-suggestion](proofing-preview-suggestion.prd.md) | Unscheduled on the roadmap (D9). The preview PRD's phases 1-4 (text layer, no sibling dependency) can run any time after the Proofing page work; phases 5-8 follow RD, EL, RC and DX-4. |
| S25 | [audacity-integration](audacity-integration.prd.md) (0-10) | D23: parallel, not gated on S22. Phases 1-2 (spikes) need the owner present with Audacity installed; phase 3 (the `DAWAdapter` extraction) can start any time after S02 (layout) and should avoid landing concurrently with an S22 phase that changes `internal/bridge`'s public surface. Phase 9 (dashboard integration) waits on S22's findings store and Review page. |

Critical chains the order respects (from the cross-PRD review): layout, then host accessor, then everything that adds a binding; harness then registry then any Lua command; spike fixture pack then EL parser superset then matcher, ledger, coverage, editing, proofing and stage signals; findings store then Review page then take review, continuity and analysis panels; Base UI foundation then every PRD that adds a dialog, popover, table or slide-over; the import review extraction (delivered by S18) then every other `Home.tsx` edit. S25 (Audacity) is independent of this chain except its Phase 9, which needs the findings store and Review page from S22.

## 5. Status

Updated by the last PR of each stack. Values: `queued`, `in progress`, `pr open`, `blocked`, `deferred`.

| Stack | Status | PRs |
| --- | --- | --- |
| S00 | in progress | this PR |
| S02 | pr open | #55, #56, #57, #58 (issue #54) |
| S03 | pr open | #61, #62, #63, #64 (issue #59; follow-up #60) |
| S04 | partial | #67 (TTS preview P1), #68 (vocabulary hints P1); issues #65, #66. Delivered: the Story Bible preview TTS PRD Phase 1 (its phases 2 and 3 were delivered by stack S16 and the PRD is deleted, see [the Story Bible preview](../architecture/story-bible-preview.md)) and [proofing-vocabulary-hints](proofing-vocabulary-hints.prd.md) Phase 1. Remaining: vocabulary Phase 2 (the tag-input box, after the primitives stack S10). Open [ADR 0042](../adr/0042-proofing-suggestions-derive-from-current-entities-and-skip-auto-extracted-needs-review.md) awaits the owner. |
| S05 | pr open | #70 to #79 (issue #69, closed by S13). Delivered: verification-and-code-health-tooling phases 1 to 8, 10 and 11: Black dropped, golangci-lint v2, the coverage ratchet ([ADR 0043](../adr/0043-coverage-is-a-ratchet-on-logic-directories-not-a-blanket-80-percent.md)), Hypothesis, fast-check and Go fuzz tests ([ADR 0044](../adr/0044-property-and-fuzz-tests-are-deterministic-in-the-gate.md)), Knip at zero ([ADR 0045](../adr/0045-dead-code-is-gated-at-zero-with-reasoned-ignores.md)), Go and Python import rules ([ADR 0046](../adr/0046-architecture-rules-taken-from-adrs-are-lint-and-test-rules.md)) and Playwright traces. Phases 9 and 12 were delivered by S13 (below), and the PRD is deleted.
| S07 | pr open | #81 to #85 (issue #80, closed by S13). Delivered: test-flakiness-and-visual-suite-stability phases 1 to 5: the `ProjectPicker` focus flake fixed at its cause (5 of 48 runs failed under load, 174 of 174 pass) and the full frontend suite hardened (Testing Library `asyncUtilTimeout` 5 s, `testTimeout` 15 s; 60 of 60 consecutive full runs), the visual drivers wait for their page and can be stress-tested with `UI_CPU_THROTTLE`, the kit scaffold gets `clickNav` (kit 0.3.2), a `-cpu 1,4` Go guard (`test-schedules`) and the `Elapsed` assertion fix, and `ci-and-releases.md` matches the live settings (no required status check, D11) and the job names that exist. Phase 6 was delivered by S13 (below), phase 7 (the pixel baseline spike) is deferred to issue #153 (after 50 consecutive stable CI runs), and the PRD is deleted. No new ADR.
| S09 | partial | #163 (phase 3, the Lua harness), #164 (phase 4, the command registry), #165 (phase 2, the event fan-out and a run ID on every `ERROR`), #166 (phase 5, spike S0 and the fixture pack) and this PR (issue #162, closed by this PR; the owner-only steps are issue #167). Delivered: [reaper-automation-follow-through](reaper-automation-follow-through.prd.md) part 1, phases 1 to 5; the PRD stays with accurate statuses for stack S22. Phase 3: a Lua 5.4 harness (fake `reaper`, the real file protocol, 94 tests, 19 mutation checks) that `pnpm check` and a Linux and Windows `lua` CI job run, with Lua from the `lupa` wheel pinned in `uv.lock` ([ADR 0066](../adr/0066-the-lua-bridge-is-tested-by-a-harness-under-lua-5-4-and-reaper-api-behaviour-is-checked-in-reaper.md), supersedes point 3 of ADR 0031, [reaper-bridge](../architecture/reaper-bridge.md)). Phase 4: commands registered by name, one Lua file per feature ([ADR 0067](../adr/0067-bridge-commands-are-registered-by-name-and-each-feature-lives-in-its-own-lua-file.md)). Phase 2: `bridge.Client` `Subscribe` and `Dispatch` in place of the single-cursor `ReadEvents`, the transcript service as the first subscriber, and a run ID on every `ERROR` ([ADR 0068](../adr/0068-bridge-events-fan-out-to-subscribers-by-tag-and-run-and-every-error-names-its-run.md)); this also fixed the transcript service silently dropping every `ERROR`. Phase 5: spike S0 in a real REAPER 7.80 ([result](../research/reaper-spike-s0-item-extension-data.md)): item `P_EXT` is an `<EXTI` block that survives save, reload, split and duplicate, and stamping a copy of a real project added only those blocks; the REAPER-saved fixture pack is under `apps/desktop/internal/tracks/testdata/reaper/`. Phase 1 is `partial`: 37 scripted checks against the real bridge pass, and they found and fixed a bridge bug (`reaper.EnumerateFiles` caches its listing). Remaining: phases 6 to 25 (stack S22), the parser superset that reads the first take of a multi-take item today (evidence ledger stack S23). Owner steps (issue #167): a real paste of a stamped item and Transcript Compare through the app with the launcher; spikes S1, S3 and S4 (audio hardware); the first CI runs of the `lua` job on Linux and Windows (local runs were Windows only, and `go test -race` runs only in CI). One thing to know: REAPER opens the default Windows audio device on start even when the first-run prompt is answered No (nothing recorded, armed or played); the spike scripts now close it first and assert it is closed. No Proposed ADR. |
| S10a | pr open | #87, #91, #99, #100, #101, #102 and this PR (issue #86, closed by this PR). Delivered: base-ui-primitive-foundation phases 1 to 6, and its PRD is deleted. Phase 1: `@base-ui/react` and the import boundary (`baseUiBoundary.test.ts`, a Vitest scan; ADR 0047). Phase 2: the dialog family, modal, alert-dialog confirms, the six red confirms (ADR 0048). Phase 3: tooltips to WCAG 1.4.13 and the info-icon popover (ADR 0049), `Field` and `MeterBar` (ADR 0050). Phase 4: `SlideOver` and `NavDrawer` (ADR 0051). Phase 5: `Pill`, `Menu`, `Checkbox`, `Collapsible`, `Switch` (ADR 0052). Phase 6: sweep, bundle budget, steady-state docs. Also delivered from other PRDs: dialog PRD phase 4 and the progress semantics of its phase 3; component-accessibility PRD phases 1 and 2 and the `Field` part of 3. Remaining in stack S10 (S10b): ui-primitives-and-headless-library (PRD deleted, delivered) phases 1, 4 and 5. Now unblocked (S13 tail): verification-and-code-health-tooling phase 9 (UI architecture rules, an ESLint second guard for the Base UI boundary) and phase 12 (aria snapshots of the dialogs, slide-overs and navigation). Owner steps: a screen-reader pass (NVDA in WebView2) on the dialogs and info icons, a look at the hamburger navigation on a narrow window, and whether to add the ESLint `no-restricted-imports` block by hand. |
| S10b | pr open | #105 (phase 1a), #108 (1b), #112 (4a), #113 (4b), #114 (5) and this PR (issue #104, closed by this PR). Delivered: ui-primitives-and-headless-library phases 1, 4 and 5, and its PRD is deleted (phases 0, 2 and 3 were absorbed by S10a). Phase 1: `IconButton` and the `rawNatives.test.ts` ratchet, then `TextField`, `SearchField`, `Select` and `Field` for textareas, and the global form CSS removed (ADR 0053). Phase 4: `Tabs` and `ToggleGroup` (ADR 0054) and `TagInput` (ADR 0055); `Popover` was not built (no caller). Phase 5: `Table` and the end of `table.dtable` (ADR 0056); the alias combobox stays bespoke. Every page control is now a primitive: no native select, input, textarea or table part outside `primitives/`, and 17 raw `<button>`s in 9 files remain as a ratchet. Owner steps: a screen-reader pass on the tab strips, the tag input and the table rows (NVDA in WebView2); the header colours (`--text-muted`, `--accent-strong`) and the selected Settings category colour are stopgaps for the palette stack S12; `settings/reset-override` is declared `sameAs` until the settings mobile layout stack (S12) fixes the squeezed row and makes it a real state. |
| S11 | pr open | #118 (dialog phase 3), #120 (component phase 3), #121 (component phase 4 adoption) and this PR (issues #116 and #117, both closed by this PR). Delivered: the dialog PRD's phase 3 (the "This step cannot be cancelled" notice for a running job with no Cancel, no empty action row, `motion-safe:` on the progress fill; owner decision D8, [ADR 0057](../adr/0057-a-running-job-that-cannot-be-cancelled-keeps-its-dialog-blocking-and-says-so.md)) and phase 5 (the sweep and steady-state docs), and the component accessibility PRD's phase 3 (`Heading` `level`, `Panel` `title` and `actions`) and phase 4 (the `Panel` title at four call sites, the sweep, [ADR 0058](../adr/0058-heading-has-a-level-and-panel-names-its-region-with-a-level-2-title.md)); both PRDs are deleted. D12 (the six red confirms) was re-checked against the code. Found and fixed on the way: a legacy `.progressbar > div { transition }` rule beat `motion-safe:`, and a long file name as a `Heading` subtitle overflowed 449 px at the narrow viewport. Not built: the inline (flowing text) tooltip, which moves to the teleprompter flags phase ([teleprompter-manuscript-integration](teleprompter-manuscript-integration.prd.md) phase 7). Owner steps: a screen-reader pass (NVDA in WebView2) that listens for the notice when the Story Bible rebuild dialog opens and for the named regions on Tracks, Proofing and the Teleprompter; a look at the Proofing chapter title, now semibold like the other panel titles. For the palette stack S12: the new notice is `--text-muted` text on the dialog surface, and the three other progress fills (Home import preview, Proofing comparison, Story Bible download) still ease under reduced motion ([motion-and-animation](../design/motion-and-animation.md)). |
| S12a | pr open | #124 (phase 1), #125 (2a), #127 (2b), #134 (2c), #131 (2d), #132 (2e), #133 (3) and #140 (4), all merged, then #141 (5) and this PR (issue #123, closed by this PR). Delivered: palette-contrast-wcag-aa, all six phases, and its PRD is deleted; see [ADR 0059](../adr/0059-text-colours-meet-wcag-aa-with-two-text-levels-a-non-text-token-and-derived-on-tint-text.md) (with its updates for phases 4 and 5) and [colour and contrast](../design/colour-and-contrast.md). Two text levels and a non-text token (`--text-faint` deleted, its 67 usages triaged in five slices), the active navigation item in `--accent-strong`, derived highlight and badge text with dark category tokens, text-safe status colours, readable placeholders, a permanent token-pair guard (`paletteContrast.test.ts`) with an empty ratchet, and the atlas debt list at 0; a scratch axe pass over every app state in both themes is at 0 violations, and the documentation images and `docs/ui` were regenerated once. Judgement calls for the owner: the derived text mix is 45% not 50% (nested highlights measured 4.4:1 at 50%), the light `--warn-text` is `#755507`, and the dark Lore, Item, Event and Note hexes. Follow-ups filed: #126 (the visual suite's `freezeClock` races its own 10 ms margin), #138 (the note colour default is written inline and hides the dark token), #139 (highlights nested three deep). Owner steps: look at the light warn text and the dark palette in the running app. Test flakiness phase 6 (axe on app states) is unblocked. S12b (settings mobile layout) stays queued. |
| S12b | pr open | #145 (Save sends only changed fields), #146 (phase 1, the responsive row), #147 (the `bool` kind), all merged, then #151 (phase 2, the collapsed-control check) and this PR (issue #143, closed by this PR). Delivered: settings-mobile-layout, all three phases, and its PRD is deleted; see [ADR 0060](../adr/0060-the-visual-suite-fails-a-collapsed-control-and-a-row-may-declare-one-narrow-on-purpose.md), [ADR 0061](../adr/0061-settings-states-are-also-captured-at-a-390px-reflow-width.md) and [Settings rows](../design/design-system.md#settings-rows). The Settings row stacks below `md` and every select is the same width from `md` up; the visual suite fails a text box or select under 64 px (kit 0.3.3, opt-out `narrowControls` with a reason); `settings/reset-override` is a real state (its `sameAs` is gone); the `bool` Settings kind (Switch) exists for D8 and no field uses it yet. Found on the way and fixed first: Save sent every field, so it failed in any scope with an unset field. Sweep of every state at 390 px: no other collapsed control; #148, #149 and #150 filed for layout decisions. Owner steps: review the Proposed [ADR 0061](../adr/0061-settings-states-are-also-captured-at-a-390px-reflow-width.md) (a 390 px capture for the 13 Settings rows only, reversing part of ADR 0037); look at the Settings page in a narrow window or at 400% zoom. Still open for the first bool field: its default in `config/defaults.json` and `builtinDefaults`, and its visual state. |
| S13 | pr open | #154 (verification phase 9), #158 (flakiness phase 6), #160 (verification phase 12) and this PR (issues #69 and #80, both closed by this PR; #153 files the deferred pixel-baseline spike). Delivered: the tail of both PRDs, and both PRDs are deleted (steady state in [Verification and code-health tooling](../operations/verification-tooling.md) and [CI and releases](../operations/ci-and-releases.md#keeping-a-red-run-meaningful)). Phase 9: `dependency-cruiser` with three import rules run by the `architecture` target of the UI project (primitives are leaves, only `src/api` reaches `wailsjs`, Base UI only in primitives), the `<mark>` scan for ADR 0016 (a Vitest test, not an ESLint rule: the config-protection hook refuses edits to `eslint.config.js` and was not bypassed), and the one violation they found fixed ([ADR 0062](../adr/0062-ui-import-rules-are-a-dependency-cruiser-config-and-a-mark-scan-that-name-their-adr.md)). Flakiness phase 6 (kit 0.3.4): axe on every app state, measured report-only first (533 elements over 50 captures, then 122 over 44 in five rules after two one-line fixes) and gated with a declared, capped debt list of 19 (state, rule) pairs ([ADR 0064](../adr/0064-the-visual-suite-runs-axe-on-every-app-state-and-a-violation-fails-unless-it-is-declared-debt.md)). Phase 12: aria snapshots of the confirms, the add-note dialog, the chapters slide-over, the drawer, the navigation lists and the info icon, in their own Playwright run after the visual suite ([ADR 0065](../adr/0065-aria-snapshots-pin-the-role-trees-of-the-dialogs-the-slide-over-and-the-navigation.md)). Follow-ups filed: #155 (nested highlight buttons), #156 (alias typeahead wiring), #157 (portalled popups and focus guards), #159 (navigation landmarks). Owner steps: review the Proposed [ADR 0063](../adr/0063-the-proofing-diff-marks-its-own-words-and-adr-0016-covers-entry-highlights.md) (the proofing diff's own `<mark>` against ADR 0016; the recommended path is taken and the exception is allowlisted); prioritise the 19 declared axe debts; the first CI run of the aria suite on Linux is the check that role trees agree across systems; add the ESLint `no-restricted-syntax` rule by hand if a lint rule is wanted. |
| S08 | pr open | #170 (phase 1), #171 (2), #172 (3), #173 (4), #174 (5), #175 (6), #177 (7) and this PR (issue #169, closed by this PR). Delivered: boundary-schema-validation, all seven phases, and its PRD is deleted; see [ADR 0069](../adr/0069-payloads-are-validated-with-zod-behind-parsewire-and-a-wrong-shape-fails-loudly.md) and [wire contracts](../architecture/wire-contracts.md). Phase 1: `parseWire` (Standard Schema typed, Zod 4), `WireError`, the `Ready` and `Bootstrap` schemas, a real host log for `SystemReportDiagnostic`, the invalid-payload startup screen. Phase 2: the live event schemas, dropped-and-counted events and a degraded-updates notice (validation costs about 0.05 microseconds an event, so events are validated in full). Phase 3: golden payloads written by Go and Python tests and validated strictly with every mock answer, and a cast scan. Phases 4 and 5: every binding result through a schema (`decodeUnchecked` deleted), inline page errors with Retry, and one real bug found and fixed (the voice install job phase). Phase 6: `internal/persist`, corrupt files kept aside and reported by class, unreadable files never overwritten, progress parsing. Phase 7: a table of REAPER events checked before delivery. Owner steps: review the ESLint decision (the config-protection hook refused `eslint.config.js`; the cast scan test enforces the rule, add the `no-restricted-syntax` rule by hand if you want it too); note that pnpm `minimumReleaseAge` needed no exclude for `zod@4.6.5`. Deferred with an issue: an event version field on the REAPER lines (#176, stack S22). No Proposed ADR. |
| S06 | pr open | #180 (phase 1), #181 (3), #182 (2), #183 (4), #184 (5a), #185 (5b), #189 (8) and this PR (issue #179, closed by this PR; the owner steps are issue #188). Delivered: release-supply-chain-hardening, phases 1 to 5b, 6 as a docs edit plus the owner checklist (D11), and 8, and its PRD is deleted; see [ADR 0070](../adr/0070-workflow-actions-are-pinned-to-a-commit-and-zizmor-gates-the-workflows.md), [ADR 0071](../adr/0071-releases-carry-build-provenance-and-promote-refuses-a-file-the-release-workflows-did-not-build.md) and [CI and releases](../operations/ci-and-releases.md#workflow-security). zizmor (blocking, no findings) and `pinact` cover 39 SHA-pinned actions where 34 of 38 were tags, every checkout drops its credential, pnpm and Dependabot install-time settings are written down, `security.yml` runs govulncheck and OSV-Scanner (advisory), the release assets, the zip's exe and the checksums are attested and promote verifies them (`assets.mjs verify --attestations`, flags checked against real attestations from a throwaway workflow), the Piper URLs are commit-pinned with a pin-shape test. Not done, by design: phase 7 (immutable releases, #186) and phase 9 (Scorecard, #187); D7 unsigned, no signing steps; owner-only settings and the first real attested release are pending (#188). |
| S15 | pr open | #193 (phase 0: the PRD and ADR 0072), #194 (phase 1: the rename and the version), #195 (2: the check), #196 (3: the download), #198 (4: the swap and the rollback) and this PR (issues #191 and #192, both closed by this PR; #199 defers attestation verification in the app; the owner steps are #200). Delivered: release-artifact-naming phase 1 (phase 2, the version in asset names, dropped by D14) and the in-app update PRD, phases 0 to 5, both PRDs deleted; see [in-app update](../architecture/in-app-update.md), [ADR 0072](../adr/0072-the-app-updates-itself-from-this-repositorys-releases-and-never-installs-without-a-click.md) (Proposed), [ADR 0073](../adr/0073-the-executable-is-named-narration-utils-and-carries-its-version.md) and [ADR 0074](../adr/0074-the-windows-update-renames-the-running-executable-and-keeps-the-old-one-until-the-new-one-starts.md). The program is `narration-utils[.exe]` and carries its bare version (`--version`, `Bootstrap.version`, Settings > About & updates); the app reads this repository's releases through a Go-validated check (once a day at most, silent, off with `Updates.check_on_startup`, `Updates.channel` candidates or stable), downloads on an explicit click with real progress (SHA-256 from the release, a GitHub-only redirect policy, one unpacked file, hostile zips refused), and on Windows replaces itself by renaming the running program, restarting it with the same arguments and keeping the old one until the new one's first `Bootstrap` (an automatic restore if it never confirms); macOS and Linux are told only. Verified by real-program install tests and a rehearsal with two real builds (a real 0.1.0 replaced by a real 0.9.9 while running). Owner steps (#200): review the Proposed ADR 0072, a real REAPER launch of the renamed program, the first update between two published release candidates, a screen-reader pass, the first CI runs on Windows. Follow-up filed: #199. |
| S14 | pr open | #203 (phase 1, the latency baseline), #204 (2, the standard, the catalog and the ratchet), #205 (5, `job:ended` and the toast queue), #206 (3, Story Bible feedback and `pending`), #207 (4, one process per Save), #208 (6, the remaining gaps) and this PR (issue #202, closed by this PR; #209 the install flows, #210 the owner steps, #211 polish). Delivered: interaction-feedback-audit, phases 1 to 7 (phase 6's install rows stay owned by release-readiness Phase 1), and its PRD is deleted; see [interaction feedback](../architecture/interaction-feedback.md), [ADR 0075](../adr/0075-every-action-that-leaves-the-interface-acknowledges-within-100-ms-cannot-be-fired-twice-and-tells-the-narrator-when-it-ends.md), [ADR 0076](../adr/0076-a-host-job-ends-with-one-job-ended-event-the-app-announces-it-and-the-story-bible-rebuild-may-continue-in-the-background.md) (Proposed) and the [latency baseline](../research/interaction-latency-baseline.md). Measured first (Windows 11, Ryzen 9 9955HX, Defender off, dev venv and a frozen sidecar, 20 runs): every Story Bible mutation was 280 ms or more, a Save was four processes (1.2 to 1.8 s), and 264 of 310 ms of a cheap operation was a Piper import only `render-audio` needs; every Go call is under 10 ms. Then: the five rules and four tiers (ADR 0075); a 110-row catalog and a ratchet test with the silent-catch guard (14 reviewed entries); `job:ended` for six job kinds with a Zod schema and golden files, an App-level subscriber and a toast queue with sticky errors (`hostAPIVersion` stays 9); `usePendingAction` and `pending` on `Button`, `IconButton` and `ConfirmDialog`, with the Story Bible actions, the destructive confirms and the file dialog moved onto them; the Story Bible rebuild may continue in the background (ADR 0076 amends ADR 0057); a Save is one process and Piper is imported only for a preview (dev venv 1.2 s to 125 ms, frozen 1.2 s to 245 ms). Bugs found on the way and fixed: an unhandled rejection in Add to Story Bible, the rebuild dialog not dismissing itself on success, and the `toast` visual states never showing a toast. Not measured (no model, voice or manuscript, and downloading needs the owner): a real spaCy build, a Piper voice load, antivirus, a real manuscript. Owner steps (#210): review the Proposed ADR 0076, an NVDA pass on the live regions and busy buttons, a real desktop run of the job-end toast. |
| S16 | partial | #214 (phase 1, the install job and `useAssetInstall`), #215 (2, the asset manager), #217 (3, the registry and the `Assets*` bindings), #218 (4, the spaCy spike), #219 (5, the spaCy catalog and the Story Bible gate), #220 (6, Manage local assets), #221 (7, startup and legacy caches), #222 (8, the packaged smoke test), #224 (14, the Windows installer), #225 (12, README and docs), #226 (13, CI docs), #227 (9, Storybook on Pages), #228 (10, the docs generator spike), #229 (11, the public docs site) and this PR (issue #213, which stays open; the owner steps are #231, code signing is #230, follow-ups #232 and #233). Delivered: release-readiness-provisioning-and-docs-site phases 1 to 14 and, with them, phases 2 and 3 of the Story Bible preview TTS PRD, which is deleted (see [the Story Bible preview](../architecture/story-bible-preview.md)); the release-readiness PRD stays with accurate statuses because three phases are `partial` on owner steps. Phase 1: one Go install job for every asset with real bytes across files, a verifying phase, a sentence for the narrator and the cause in the host log, a second start joining the running job, and one `useAssetInstall` hook and `AssetInstallPrompt` in place of three poll loops, so the nine `owned` interaction rows became `ok` ([ADR 0077](../adr/0077-every-asset-install-is-one-job-with-real-bytes-a-second-start-joins-it-and-one-hook-follows-it.md)). Phase 2: a manifest of every file (size, hash, source, time), a listing that reads no file contents (5 Whisper models, 5.3 GB: 0.6 ms against 3.2 s), a full read once per session, verify and repair, HTTP Range resume of a cut-off download (verified against Hugging Face and GitHub), a rename-aside swap, a disk check that names both sizes, stale-folder cleanup and no temporary-folder cache fallback ([ADR 0078](../adr/0078-asset-state-comes-from-the-manifest-an-asset-is-read-in-full-once-per-session-and-a-failed-download-resumes.md)). Phase 3: a registry of providers built once and six generic bindings, the managers hoisted out of `configureLocked` ([ADR 0079](../adr/0079-every-downloadable-asset-is-listed-installed-verified-and-removed-through-one-registry-of-providers.md)). Phases 4 and 5: the spike found that the frozen sidecar loads a model from an unpacked directory, then the two pinned official spaCy wheels are a third asset kind that the manager unpacks at install (hostile archives refused), the Story Bible asks before it downloads (Download, Build with rules-only for this run, Cancel) and the Settings choice comes from the catalog ([ADR 0080](../adr/0080-the-story-bible-language-model-is-a-catalog-asset-unpacked-at-install-and-the-build-asks-before-it-downloads.md)). Phase 6: Settings > Local assets ([ADR 0081](../adr/0081-local-assets-is-a-settings-list-built-from-the-registry-whose-rows-own-their-download-verify-and-remove.md)). Phases 7 and 8: a test that startup makes no download (the in-app update check is the one intended request), a developer seed command, the `--smoke` mode run by the Windows build, and the frozen Piper and dictionary data. Phase 14: a per-user NSIS installer as a release asset ([ADR 0082](../adr/0082-windows-installs-per-user-from-an-nsis-setup-program-that-wails-builds-and-the-release-carries-beside-the-update-zip.md)). Phases 12 and 13: the README, the code map, the first-use doc and the CI docs match the code and the live settings, and the unused `scripts/ci/changed-files.mjs` is gone. Phases 9 to 11: Storybook and a docs site built by MkDocs Material straight from `docs/` with a curated include list and a dead-link check ([ADR 0083](../adr/0083-the-public-docs-site-is-built-by-mkdocs-with-the-material-theme-straight-from-docs-and-a-reviewed-include-list.md)). Not done, by design: phase 15, code signing, is deferred by D7 (#230), and phase 16, the first stable rehearsal, is owner-run (#231). `partial` on owner steps: phase 9 and 11 (the deploy needs the owner to enable GitHub Pages, so no deployed site has been checked) and phase 14 (nothing was installed on a clean machine). Owner steps (#231): enable Pages, a clean-machine install and uninstall, the real downloads through the app (Piper, Whisper, spaCy: cancel, resume, repair, a full disk), a screen-reader pass on the new dialogs and the Local assets page, promoting a candidate that has the setup program, and review of `tools/docs-site/include.txt`. No ADR is Proposed by this stack. CI: `quality / ui-visual` was already red on the S14 base (a `manuscript / invalid-payload` locator flake on Linux); one flaky Go test of the cancel path is fixed in this PR. |
| S17 | pr open | #236 (phase 1, the link check), #237 (2, blocking), #241 (3, the threat model), #242 (4, the container view and self-hosted Mermaid), #243 (5, four sequence diagrams), #244 (6, the diagram parse check), #247 (7, the notices generator), #248 (8, model provenance), #249 (9, the licence allow-list), #252 (10, the notices as a release asset) and this PR (issue #235, closed by this PR; #251 is the skipped markdown lint). Delivered: docs-security-and-hygiene, phases 1 to 10, and its PRD is deleted; see the [threat model](../architecture/threat-model.md), [model provenance](../architecture/model-provenance.md), [the docs link check](../operations/ci-and-releases.md#the-docs-link-check), [third-party notices](../operations/ci-and-releases.md#third-party-notices), [ADR 0084](../adr/0084-repository-links-are-checked-offline-on-every-pull-request-and-diagrams-are-parsed-and-drawn-without-a-third-party.md) (Accepted) and [ADR 0085](../adr/0085-the-third-party-notices-are-generated-from-the-release-and-ship-as-their-own-asset-and-not-inside-the-update-zip.md) (Proposed). Phase 1 and 2: `Docs / Links (offline)` runs on every pull request with no path filter and fails on a dead repository link (proved red on a deliberate dead link, then green), a weekly online run only reports (baseline: 0 dead repository links, 7 external links, 1 real: the Pages site), a test fails when a source file cites a PRD that is not in the tree. Phase 3: 31 STRIDE rows over eight boundaries lined up with `SECURITY.md`, the update trust boundary (D14), and findings [#238](https://github.com/countrymanprime/narration-utils/issues/238) (Google Fonts and no CSP), [#239](https://github.com/countrymanprime/narration-utils/issues/239) (DOCX entries have no size cap) and [#240](https://github.com/countrymanprime/narration-utils/issues/240) (the bridge folder relies on the profile ACL); a reviewer agent found 12 discrepancies in the first draft, all fixed. Phases 4 to 6: a container view and four sequence diagrams (first-use download, live teleprompter, REAPER bridge, and the in-app update with its STRIDE notes) next to the docs that own them; the public site ships its own Mermaid because Material would load it from unpkg.com; every diagram is parsed in `pnpm check` (Mermaid's parser needs only jsdom, no browser). Phase 7: `scripts/licenses/notices.py` derives the shipped Python set from the frozen bundle (74 distributions), reads the UI and Go dependencies, and refuses to write a report it cannot trust; findings [#245](https://github.com/countrymanprime/narration-utils/issues/245) (the freeze contains hypothesis and pytest) and [#246](https://github.com/countrymanprime/narration-utils/issues/246) (three components ship no licence text). Phase 8: a generated model provenance table with a review row per artifact. Phase 9: a dependency-review licence allow-list (AGPL-compatible families in, GPL-2.0-only out). Phase 10: the notices are a release asset with a checksum, attested and verified by promote; **not** inside the update zip (ADR 0085 Proposed, [#250](https://github.com/countrymanprime/narration-utils/issues/250)). Not done, by design: phase 11 (markdown lint, #251). Owner steps: review Proposed ADR 0085; the first `Build (Windows)` run of the new notices step (this stack's own pull requests are the first CI runs); a throwaway pull request to `main` that adds a dependency with a licence outside the allow-list, to see dependency review flag it (the job only runs for pull requests to `main`); whether libx264 and libx265 make the FFmpeg build inside PyAV GPL (recorded as not verified); requiring `Docs / Links (offline)` in the ruleset (optional, owner-only); look at the five diagrams on GitHub (the local browser pane could not take a screenshot). |
| S18 | pr open | #256 (phase 1, the mock, the tests and the extraction), #257 (2, the subtitle), #258 (3, the grouped summary) (issue #255, closed by the last pull request). Delivered: import-review-redesign, phases 1 to 3, and its PRD is deleted; see [the import review](../architecture/import-review.md) and [ADR 0086](../adr/0086-the-import-review-is-grouped-by-what-each-section-will-be-and-reports-repairs-instead-of-a-log.md) (Accepted). Phase 1: a realistic mock preview (sections of every kind, three suggestions, a Markdown variant and a repaired one), 13 Vitest tests through `Home` that also pass against the unchanged dialog, and the review body moved into `ImportReview.tsx`. Phase 2: `DraftSection.subtitle` (the first paragraph's, the one the written chapter gets; additive, `hostAPIVersion` stays 12) shown after the title. Phase 3: a summary of narration chapters (front matter and the rest on a second line), `Disclosure` groups with counts and the notes as info icons, the importer's repairs (`notices`) instead of the preview activity panel, Select all and none, and the seam for the D8 "Build the Story Bible after import" checkbox (drawn only when handed; the state and the chaining are stack S19). Bugs found on the way and fixed: the preview log scrolled but was not keyboard reachable, a long subtitle pushed the selects out of the dialog (a `<fieldset>` does not shrink), and an info icon opened by hover inside a dialog did not close on Escape. Owner steps: an NVDA pass on the groups and the info icons, and the review on the user's own manuscript (the fixtures use the "CHAPTER ONE / Bad Ideas Look Great in Neon" shape). No Proposed ADR. |
| S19a | pr open | #260 (phase 1, properties), #261 (phase 2, mode-based actions), and this PR (issue #259, closed by this PR). Delivered: story-bible-entries-and-actions, all three phases, and its PRD is deleted; see [Manuscript Guide](../utilities/manuscript-guide.md), [Story Bible](../guides/using-the-app/story-bible.md) and [ADR 0087](../adr/0087-story-bible-header-actions-are-mode-based-and-lock-cannot-happen-mid-edit.md) (Accepted, supersedes the "regardless of mode" clause of ADR 0018). Phase 1: an ordered `properties` list (`{key, value}`) on every entity, the sidecar's `edit`/`create --properties`/`merge`/`merge_locked` carry-over, the Go `normalizeEntity` default, and a `PropertiesSection` in `GuideDetail` and `EntitySummary`. Phase 2: mode-based header actions (Lock in the read view only, a red separated Delete in edit mode only), which also removes the mid-edit lock hazard ADR 0018 left open, and an icon-only "Go to line". Phase 3: `pronounce_source`/the `pronounce` command (CMU or eSpeak explicitly, refused on a locked entity, marked `chosen` so `merge_locked` keeps it), the `GuidePronounce` binding (`hostAPIVersion` 12 to 13), and Play gated on a pronunciation existing via `IconButton`'s new `disabledReason` (D6, WCAG 1.4.13) and `Menu`'s new `render` prop for an icon-only trigger. Generate/Replace are scoped to the canonical name for this stack; the binding already carries an alias index for a later phase to extend to aliases. No owner steps pending. |
| S19c | partial | #265 (phase 1, stop the leaks; a review fix landed in the same PR), #266 (phase 2, the Contents/Characters decision and ADR), #268 (phase 3, structured character properties) and #271 (phase 4, TOC as the chapter list; issue #264, which stays open). Delivered: import-structure-toc-and-characters, phases 1, 3 and 4 in full, phase 2 storage-side; the PRD stays (not deleted) because phases 2 and 4 are `partial`. Phase 1: `characterListActive` now ends at any heading whose outline level is as shallow as or shallower than the Characters heading's own (not only a narrative-marker-shaped one), and Word's "TOC Heading" style is recognised despite its `outlineLvl` 9, fixing the "Contents becomes a character candidate" bug and a pre-first-chapter or Title-adjacent TOC leaking into Front Matter or narration; a go-reviewer pass found and fixed one HIGH bug (a nested character-matching subheading re-baselining the scope's level). Phase 2: settled via [ADR 0088](../adr/0088-contents-and-characters-stay-reference-hidden-by-the-reader.md) - both were already `contentKind: reference`, so only the decision, a regression test and the ADR were needed; hiding them from the reader's own page-flip view (not just the nav panel) is manuscript-reader-search-and-controls Phase 5 (stack S19b), which had not landed. Phase 3: a `characterCandidateScanner` implementing the structural label rule (S4) replaces the stateless `characterLine` check - a bare name or heading opens a candidate, a `Label: value` line attaches as a property to whichever is open - wired to `Service.CreateFull` (built by S19a for exactly this); a go-reviewer pass found and fixed two bugs (CRITICAL: a duplicate name fabricating a phantom candidate from the next stray label; HIGH: a flat cast-list group not flushing a candidate left open by the prior group). Phase 4: a docx's own table of contents becomes the authoritative chapter list above an 80% match threshold ([ADR 0089](../adr/0089-a-docx-table-of-contents-becomes-the-authoritative-chapter-list-above-a-match-threshold.md)), matched by bookmark then text then position; only `chapterTitles` is ever overwritten, never `Sections`; a go-reviewer pass found and fixed one HIGH bug (an ordinary in-body cross-reference hyperlink miscounted as a TOC entry). Not delivered, deferred: Markdown TOC recognition (PRD marks it "Should"). No real manuscript with a Word TOC field was available; Phase 4 is verified against a hand-built OOXML fixture with real `_Toc` bookmarks, recorded as awaiting one. Owner steps: review Proposed status is not needed (both new ADRs are Accepted, not Proposed, since D22 already answered the qualitative questions); look at the 80% TOC-match threshold (ADR 0089) once a real manuscript is available. |
| S19b | pr open | #275 (phase 1, search fixes), #276 (phase 2, debounce and subset), #279 (phase 3, result rows), #281 (phase 4, controls bar), and this PR (phase 5, issue #272, closed by this PR). Delivered: manuscript-reader-search-and-controls, all five phases, and its PRD is deleted; see [the manuscript guide](../guides/using-the-app/manuscript.md) and [ADR 0090](../adr/0090-the-page-flip-reader-hides-reference-chapters-superseding-the-reader-half-of-adr-0005.md) (supersedes the reader-view clause of [ADR 0005](../adr/0005-reference-material-excluded-from-chapter-nav.md); the navigation-panel clause is unchanged). Phase 1: `paragraphIds`-based line numbers for collapsed chapters, "No matches" when every hit is hidden, selecting a result and the clear icon both clear the query, `JUMP_HIGHLIGHT_MS` halved to 30s, one `--bookmark` token for the header and panel. Phase 2: `SEARCH_DEBOUNCE_MS` (2s, Enter fires immediately), a free client-side chapter-title/subtitle subset ahead of the debounce, a "Searching…" hint, and the stale-response guard kept. Phase 3: `windowExcerpt`, a dedicated `Search` `Highlight` kind on the delivered colour-mix infrastructure, and a subtitle line in each chapter row. Phase 4: one right-aligned controls cluster (text size left, Chapters & Search plus expand/collapse right, `ml-auto`, wraps only below 400px), `Tooltip`'s new `icon` prop carries the `faFont` "Text size" trigger, doc screenshots regenerated. Phase 5: the continuous reader reuses the existing `isListableChapter` predicate (no second `isRecordedChapter` helper) to hide every `reference` chapter from its render loop, the default open chapter (on load and for a saved reader state that now points at a hidden chapter, R14, existing manuscripts included) and "Expand all"; a `#p`/`#c` deep link into a reference chapter is a no-op `notify` message rather than a redirect, since the reader has no standalone reference-chapter view to land on instead. This also completes import-structure-toc-and-characters Phase 2, which was waiting on it (that PRD's row updated separately, still `partial` on its own Phase 4). Owner steps: none pending. |
| S01, S19d-S24 | queued | |
| S25 | queued | Not gated on S22 (D23); phases 1-2 need the owner present with Audacity installed and scripting enabled before anything else in the stack starts. |
