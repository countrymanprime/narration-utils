# Threat model

**Status: reviewed 2026-09-21** (host API 12, ADRs up to 0083, the release-readiness stack merged); the no-listener statement, rows 3d and 3e (new) and 8b re-read on 2026-09-25 for the Wails v3 migration (ADR 0200, host API 49); row 6j added 2026-09-25 for the MP3 container check (delivery profiles Phase 6); row 3 and the table of requests re-reviewed 2026-09-23, when the fonts were bundled (#238); row 4e added 2026-09-23 for the recording-coverage sidecar mode and updated the same day when the Go coverage service started it, when the UI got its bindings, and when its regions gained `before` and `after` bounds (model cascade Phase 2, ADR 0168), and re-read 2026-09-25 when it gained the word alignment and `--align-only` (ADR 0242); row 4f added 2026-09-23 for the per-take divergence sidecar mode, and re-reviewed 2026-09-23 when the Review page's take comparison started it (take review Phase 10). Rows 6f and 8b re-reviewed 2026-09-23 for the Audacity launcher shortcut (ADR 0145); rows 5b and 5d re-reviewed 2026-09-23, when the bridge learned to go to and loop a finding (review dashboard Phase 6); row 5f added 2026-09-23, when the Review page started sending those commands (Phase 7); row 5g added 2026-09-23, when the Review page started adding an approved marker (Phase 8); rows 4a and 5f re-reviewed 2026-09-23, when the pickup and duplicate scan moved to the Review page as a job and each read of a group got its own Go to and Loop (take review Phase 5); row 5h added 2026-09-23 for the cleanup launchers (reaper follow-through Phase 23); row 5i added 2026-09-23 for the retake-lane pick (reaper follow-through Phase 25); row 6g added 2026-09-23 for measuring the files the narrator picks (diagnostics Phase 1, ADR 0156), and re-reviewed 2026-09-23 when the Diagnostics tab started checking the same files (diagnostics Phase 6); row 6h added 2026-09-23 for the Delivery report the app writes (diagnostics Phase 7); row 6a re-reviewed 2026-09-24 for `credits.Detect`'s second read of the project's own stored manuscript source (credits-token-setup-and-front-matter-detection.prd.md Phase 1); row 6i added 2026-09-24 for the delivery profiles the app reads back (delivery platform profiles Phase 4, ADR 0180). Row 8a re-reviewed 2026-09-24, when the Prerelease stopped re-running the quality jobs.; row 6k added 2026-09-25 for the teleprompter's per-chapter reading file (read-aloud-resume-from-daw PRD Phase 2, ADR 0205); row 6d re-read 2026-09-25 when chapter sync started re-parsing the linked `.rpp` after each save, and row 5f re-read the same day and unchanged, since that watcher sends REAPER no command (auto-sync Phase 4, ADR 0210). This is a table the maintainer reads, not a diagramming tool: [STRIDE](https://learn.microsoft.com/en-us/azure/security/develop/threat-modeling-tool-threats) letters per row, the mitigation that exists in code with the file that holds it, and the risk that is left with an owner. It lines up with [`SECURITY.md`](../../SECURITY.md) row by row: every scope bullet there has a row here, and the two boundaries `SECURITY.md` does not list (the REAPER file bridge, the `/media` route) are here because they are real. It is engineering guidance, not a certification and not legal advice.

**Keeping it true.** A change that touches a trust boundary below updates this file in the same pull request: the `feature-cleanup` skill and the pull request template both ask "does this change a trust boundary or what the app sends off the machine?". Bump the date above when a row is re-read against the code, not only when it changes. A finding that is not fixed here is a GitHub issue ([Tracking work on GitHub](../operations/github-workflow.md)), named in its row.

## How to read it

| Letter | Threat | | Letter | Threat |
| --- | --- | --- | --- | --- |
| S | Spoofing: pretending to be a server, a file or a process | | I | Information disclosure |
| T | Tampering: changing bytes in transit or at rest | | D | Denial of service |
| R | Repudiation: no record of what happened | | E | Elevation of privilege |

The attacker is one of: **a network attacker** (or a compromised server) between the app and GitHub or Hugging Face; **a hostile file** the narrator opens (a manuscript, a REAPER project, an archive); **another process of the same Windows user** (there is no privilege boundary between two programs a person runs, so this is a fact to record, not a fight to win); **the supply chain** (a dependency or a workflow change that reaches a release). Local-first is the design: the narrator's manuscripts, audio and Story Bible stay on their machine, and nothing here assumes a server of ours, because there is none.

The boundaries are drawn in the [container view](codebase-map.md#how-the-parts-connect), and the four flows that cross them are drawn step by step beside the doc that owns each: [the first-use download](first-use-dependency-provisioning.md#the-install-step-by-step), [a live teleprompter session](manuscript-teleprompter.md), [a REAPER bridge command](reaper-bridge.md#one-command-step-by-step) and [the whole update](in-app-update.md#the-whole-update-step-by-step) (each names the row of this model that a step answers).

## What is protected

| Asset | Where it lives | Why it matters |
| --- | --- | --- |
| Manuscripts, the Story Bible, notes | the project's `narration-utils/` folder next to the REAPER project | unpublished writing of the narrator or the author |
| Recorded audio and the `.rpp` project | the narrator's folders, read (never modified) by the app; REAPER edits the project through the bridge | the product being made |
| Settings, recents, host log | `%APPDATA%\narration-utils` and the per-user cache | small; the host log records where data went wrong and never what the data was ([`hostlog`](../../apps/desktop/internal/hostlog/hostlog.go)) |
| Downloaded models, voices and the dictionary | `%LOCALAPPDATA%\narration-utils\assets` | a wrong model gives wrong text, and a weight file is parsed by native code; the dictionary's dataset is parsed, and its lookup index read, by the Go host itself |
| The program itself | the install folder, replaced in place by the update | code that runs with the narrator's rights |
| The release | GitHub Releases | what every narrator installs |

## What the app sends off the machine

The statements below are the ones the docs make elsewhere; each is checked by something that fails.

- **No telemetry, no analytics, no crash upload, no account.** Nothing in the host or the UI reports usage. The host log stays on disk (`hostlog`, [ADR 0069](../adr/0069-payloads-are-validated-with-zod-behind-parsewire-and-a-wrong-shape-fails-loudly.md)); analyzers report findings and never upload them ([ADR 0032](../adr/0032-analyzers-report-findings-and-never-change-audio-or-manuscript-on-their-own.md)).
- **No listener.** The desktop host opens no port and no socket. The `/media` route is the Wails asset server's in-process handler, reached only by the app's own webview ([ADR 0012](../adr/0012-media-route-for-track-playback.md)); so are the page's binding calls and events on Wails v3, which are requests to `/wails/runtime` on `http://wails.localhost` that WebView2 hands to the host in-process (`WebResourceRequested`), and v3's server mode, the only one that listens on a port, is behind a `server` build tag the build never sets ([ADR 0200](../adr/0200-the-desktop-shell-runs-on-wails-v3-beta-pinned-at-v3-0-0-beta-25.md)); the REAPER bridge is files in a folder ([ADR 0031](../adr/0031-reaper-integration-is-a-lua-file-bridge-verified-by-hand.md)).
- **The Go host imports networking in three places only:** `internal/assets` (first-use downloads), `internal/update` (the app's own update) and the root `media.go` (the local route). A `depguard` rule in [`.golangci.yml`](../../apps/desktop/.golangci.yml) fails the lint on any other import of `net`, `crypto/tls` or `golang.org/x/net` ([ADR 0032](../adr/0032-analyzers-report-findings-and-never-change-audio-or-manuscript-on-their-own.md) point 4). The fence does not see a sidecar's own network use, which is why the sidecars are loaded from a verified local directory (row 1).

Every outbound request the shipped app can make:

| Request | When | Carries | Off switch |
| --- | --- | --- | --- |
| `GET api.github.com/repos/countrymanprime/narration-utils/releases` | automatically once a day at most (2 hours after a failed check), 12 s after the window is up, and when the narrator presses **Check now** (row 2) | the program name and version in `User-Agent`, an `ETag`; nothing about the narrator, the project or the machine | Settings > About & updates > `Updates.check_on_startup` |
| Hugging Face and GitHub asset downloads | only after the narrator confirms an install (row 1) | the file path of one pinned URL | not started without a click |
| The update download from GitHub Releases | only after two clicks (row 2) | the URL of one release asset | not started without a click |

A developer running a sidecar by hand with a model *name* instead of `--model-dir` makes `faster-whisper` download from Hugging Face by itself (`sidecars/transcript-compare/core/compare.py`, `sidecars/manuscript-teleprompter/core/live_asr.py`, `local_files_only=bool(model_dir)`), and the Moonshine engine of the teleprompter sidecar, run from source, lets the `moonshine-voice` library download its model from Moonshine's servers when it is given no `--model-dir`. The frozen sidecar that ships cannot: it refuses to run Moonshine without a `--model-dir` and checks that directory holds every file the catalog pins, word-timestamp decoder included (`sidecars/manuscript-teleprompter/core/moonshine_engine.py`, [ADR 0107](../adr/0107-moonshine-ships-inside-the-windows-teleprompter-sidecar-and-runs-only-from-a-verified-catalog-install.md); the host passes it the verified catalog directory of whichever engine the narrator chose, teleprompter-engines-and-input-devices PRD phase 7). The mock-API bundle of the UI (`VITE_USE_MOCK_API=1`, development, the visual suite and the public demo) makes no request either: its sample manuscript, the Project Gutenberg text of *Alice's Adventures in Wonderland*, ships with the bundle (`apps/ui/src/api/fixtures/`, loaded by `aliceManuscript.ts`). The app never does: the host resolves the model directory from an installed, verified catalog entry and refuses with `asset_required` otherwise (`apps/desktop/bindings.go`). The start-up test `apps/desktop/startup_offline_test.go` replaces the HTTP transports with ones that fail, and asserts that opening the app, choosing a model and reading the catalog make no request except the update check, and that one only to the releases address and only when it is switched on.

## The threats

Rows are numbered so an issue or a pull request can name one. "Cited" files are relative to the repository root.

### 1. First-use downloads (`SECURITY.md` bullet 2)

Boundary: Hugging Face and GitHub to the asset store to a sidecar that loads the file. Flow: [first-use dependency provisioning](first-use-dependency-provisioning.md).

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 1a | T, S: a tampered or redirected model, voice, spaCy wheel or dictionary archive | The catalogs are compiled into the release (`config/*-assets.json`); every file has an exact size and SHA-256; the Hugging Face files are pinned to a 40-hex commit on `https` and `apps/desktop/internal/assets/pins_test.go` fails a Whisper or voice catalog entry that is not (a tag, a branch, `http`, a missing hash); the two spaCy wheels and the Open English WordNet archive (`config/dictionary-assets.json`, the `2025-edition` release asset) are pinned by release tag and SHA-256 and `internal/spacy/catalog_test.go` and `internal/dictionary/catalog_test.go` check their URLs; the download goes into a `<dir>.installing` staging folder and is renamed into place only after size and hash match (`internal/assets/store.go`); a cut-off download resumes with an HTTP Range request and is re-hashed whole; archives are unpacked by our code with hostile entries refused (`internal/assets/extract.go`); the sidecars load the model from that directory with `local_files_only`, and the frozen teleprompter sidecar loads Moonshine only from such a directory, never through the library's own downloader (which checks CRC32C, not SHA-256; [ADR 0107](../adr/0107-moonshine-ships-inside-the-windows-teleprompter-sidecar-and-runs-only-from-a-verified-catalog-install.md)) | A hash in the same commit that names the file proves the file is the one we pinned, not that upstream's file is benign; the pin was checked against the live upstream when it was recorded and a new pin is a reviewed change. Owner: maintainer |
| 1b | D: a hostile server sends endless data or a huge `Content-Length` | The size in the catalog is checked against the free disk before the first byte is written and the body is read no further than it; the hash gate rejects the rest ([ADR 0078](../adr/0078-asset-state-comes-from-the-manifest-an-asset-is-read-in-full-once-per-session-and-a-failed-download-resumes.md)) | A slow server can hold a job open until the narrator cancels. Accepted |
| 1c | E: the downloaded file is code | A model file is data loaded by `ctranslate2`, `onnxruntime`, `spacy` and, on Windows, `moonshine-voice`'s native `moonshine.dll` and the ONNX Runtime it bundles; the only executables in the app come from the release, not from a download (row 2 covers the release) | A malformed model could exploit a parser bug in those libraries; they are pinned in `uv.lock` and scanned by OSV-Scanner. Owner: maintainer |
| 1d | R: no record of which model produced a result | The manifest in each install folder keeps the URL, size, hash and time; Settings > Local assets lists and verifies them ([ADR 0081](../adr/0081-local-assets-is-a-settings-list-built-from-the-registry-whose-rows-own-their-download-verify-and-remove.md)) | None worth a row |
| 1e | D, T, E: the dictionary (a file the app opens) is parsed and read inside the desktop host, not a sidecar | Selecting or looking up a word never downloads: `SystemLookup` answers `asset_required` and only the confirmed `AssetsInstall("dictionary", …)` fetches the archive (`apps/desktop/dictionarylookup.go`). The dataset is parsed once, at install, only after its size and SHA-256 matched, by `encoding/json` (memory-safe Go) inside the staging folder; a release that is not in the expected shape fails the install with `ErrBadContent` and nothing is kept (`internal/assets/derive.go`, `internal/dictionary/build.go`); the unpack caps entries and bytes as for any archive (row 1a). Only the built index is kept, recorded in the manifest with its own hash, and read in full against it on the first lookup of each session. Every lookup bounds its reads: the tables must fit the file, a record is at most 1 MiB and must lie inside the blob, and a bad record is `ErrDamagedIndex`, never a panic or a large allocation (`internal/dictionary/index.go`, `index_test.go` feeds it truncated, garbled and oversized headers). The selection is capped at 64 characters and must be one word; the index file is opened per lookup and closed, so Remove and Repair are never blocked | Building the index takes about 300 MB of memory for a second or two at install (measured on the 2025 release); a file changed on disk after the session's first lookup is still read with the same bounds. The definitions are third-party text: the reader's look-up panel renders every field as React text, never as HTML, and the download is asked for in the standard first-use question (`apps/ui/src/components/manuscript/WordLookup.tsx`; `WordLookup.test.tsx` feeds it markup and finds none in the page). Owner: maintainer |

### 2. The app's own update (`SECURITY.md` bullet 3)

Boundary: GitHub's API and CDN to the app, which then replaces its own executable ([in-app update](in-app-update.md), [ADR 0072](../adr/0072-the-app-updates-itself-from-this-repositorys-releases-and-never-installs-without-a-click.md) to [ADR 0074](../adr/0074-the-windows-update-renames-the-running-executable-and-keeps-the-old-one-until-the-new-one-starts.md)). This is the boundary where a mistake runs an attacker's program with the narrator's rights, so it has the most rows.

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 2a | S: a spoofed or hostile release list (a man in the middle, a hijacked response, a poisoned cache file) | TLS to `api.github.com`; the repository is compiled in (`update.Repository`), so no setting or response can point elsewhere; the body is capped (4 MiB), read as at most 60 entries, and every release is validated in Go (a tag only as `vMAJOR.MINOR.PATCH` or with `-rc`, exactly one asset with the platform's name for the tag's version and one `.sha256`, sane sizes); every download URL is **built** from the repository, the tag and the known name, never taken from the JSON; the parsing is fuzzed (`internal/update/manifest.go`) and the on-disk cache is re-validated like a fresh answer on every read (`internal/update/check.go`) | A compromise of GitHub itself, or of the release, is outside what the app can see (2c). Owner: maintainer |
| 2b | T: the downloaded zip or its checksum is changed in transit or on the CDN | HTTPS only; redirects only to `github.com` and the release-asset hosts on port 443, at most five (`DownloadRedirectPolicy`); if the `.sha256` names a file it must be this release's zip, and if GitHub lists a digest for the zip the two must agree (when GitHub lists none, the check rests on the `.sha256` alone); the zip is read no further than its declared size into a staging folder and renamed only after size and SHA-256 match; exactly one entry, `narration-utils.exe`, is unpacked (a regular file, bounded, no path or link) (`internal/update/stage.go`) | The checksum sits in the same release as the file, so it detects damage, not a substituted release (2c) |
| 2c | T: a release that GitHub serves correctly but that is not the maintainer's build (a compromised workflow or account) | **Not verified in the app.** The release carries build provenance for every asset and the executable, checked in CI by promote (`assets.mjs verify --attestations`, [ADR 0071](../adr/0071-releases-carry-build-provenance-and-promote-refuses-a-file-the-release-workflows-did-not-build.md)); a person can run `gh attestation verify` on a download | The app installs a release that has a matching checksum and no provenance check ([#199](https://github.com/countrymanprime/narration-utils/issues/199): sigstore verification in the app, weighed at about 370 modules and 16 MB and deferred). The first stable is unsigned (owner decision D7). Owner: maintainer, tracked in #199 |
| 2d | D, T: a **downgrade** or replay: an old, vulnerable release offered as the update | A release is an update only if its bare version is strictly greater than the running one; an equal or lower version is never installed (`internal/update/version.go`); a development build (`0.0.0-dev`) is never offered one | A hostile list can offer the highest version the parser accepts (each part has at most six digits) and so pin a narrator to "no newer update"; it cannot make the app install without the two clicks, and the install runs the new program with `--version` first. Accepted |
| 2e | E: the swap is used to run or replace something else | The narrator's confirmed click is required for the download and again for the install, and the install refuses while a job runs; the staged program is copied beside the running one, hashed against the staging record, run once with `--version` and must print the version the release names; the record of an update in progress (`pending.json`) is only compared with the running program and never used as a path, so a planted record cannot make the app touch another file; the update never asks for elevation and falls back to "show the downloaded file" where it cannot write (`internal/update/install.go`, `internal/update/startup.go`) | Another process of the same user can replace the staged file between the hash and the rename (a race with the narrator's own rights; no boundary is crossed). Accepted |
| 2f | D: an update that does not start leaves the narrator without the app | The old program is kept as `<name>.old` until the new one serves its first `Bootstrap`, and is restored automatically after two starts that never confirmed | A new build that starts and is then broken is not rolled back ([in-app update](in-app-update.md#limits)). Owner: maintainer |
| 2g | I: the request identifies or tracks the narrator | The request carries `Accept`, `X-GitHub-Api-Version`, `If-None-Match` and `User-Agent: narration-utils/<version>` and nothing else; it is at most once a day, silent on failure, and off with one setting | GitHub sees the narrator's IP address, as with any request to it. Stated in `SECURITY.md`. Accepted |

### 3. The webview at start-up (`SECURITY.md` bullet 1)

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 3a | I: every launch sends the narrator's IP address and user agent to a font host, and loads a remote stylesheet | **Fixed** ([#238](https://github.com/countrymanprime/narration-utils/issues/238)): the three typefaces (Barlow Condensed, IBM Plex Mono, IBM Plex Sans) are bundled from the `@fontsource/*` packages (`apps/ui/src/fonts.ts`) and `apps/ui/index.html` names no other host; the fonts are OFL-1.1 and listed in the notices | None left at start-up. Owner: maintainer |
| 3b | T, E: the webview has no Content-Security-Policy | The bundle is built from the repository and served from the executable, and since #238 it loads nothing from another host; the UI has no `dangerouslySetInnerHTML` and no `eval` of received text (`apps/ui/src`, checked 2026-09-21) | No CSP yet. Nothing remote is left to allow, so a policy of the app's own origin is now possible; it needs checking in the packaged Windows app (the Wails runtime and the `/media` route), so it is its own change. Owner: maintainer |
| 3d | E, T: the page calls something on the host beyond the Host's bindings (Wails v3 binds every exported method of every registered service) | One service is registered, the Host (`main.go`); its lifecycle hooks are `ServiceStartup`/`ServiceShutdown`, which v3 never binds; Wails' notification service is started by hand and never registered, so its methods are not callable ([ADR 0200](../adr/0200-the-desktop-shell-runs-on-wails-v3-beta-pinned-at-v3-0-0-beta-25.md)). The page reaches the host only through `src/api/wailsClient.ts`: `wails-bindings-only-in-api` and `wails-runtime-only-in-api` fail the import graph otherwise (`apps/ui/.dependency-cruiser.mjs`). A first notification writes the per-user toast activator key (`HKCU\Software\Classes\CLSID\<id>\LocalServer32`, the program's own path), as v2 did; a click on a notification starts the program, which a second launch hands to the running one (it attaches nothing without `--project-folder`) | An exported method added to `Host` is a binding by that fact, as on v2; review sees it in the regenerated `apps/ui/wailsjs/` and in `hostAPIVersion`. Owner: maintainer |
| 3e | T, E: the webview host is a beta dependency whose IPC and build steps changed | Exact pins: `github.com/wailsapp/wails/v3 v3.0.0-beta.25` in `go.mod`/`go.sum`, `@wailsio/runtime` 3.0.0-beta.25 (no dependencies of its own) with its integrity hash in `pnpm-lock.yaml`, and the `wails3` CLI at the same version; pnpm's three-day `minimumReleaseAge` was waived for that one version by the owner (a `minimumReleaseAgeExclude` entry removed once the version is three days old); Dependabot's cooldowns; `govulncheck` and OSV-Scanner cover the module. Every v3 default that differs from v2 is set back and tested (Ctrl+wheel zoom, the browser menu, dialog cancel, error text) ([ADR 0200](../adr/0200-the-desktop-shell-runs-on-wails-v3-beta-pinned-at-v3-0-0-beta-25.md)) | A beta can change behaviour between tags, so each bump is its own reviewed change; the first launch of the v3 build on Windows is the owner's ([#510](https://github.com/countrymanprime/narration-utils/issues/510) item 1). Owner: maintainer |
| 3c | (metric) the release-readiness "no outbound request at startup" statement is measured by a Go transport test, which cannot see the webview | `startup_offline_test.go` counts the host's requests; the webview's only remote request (3a) is gone | The webview itself is not measured by a test; `apps/ui/index.html` and `src/fonts.ts` are the places a remote request would be added. Owner: maintainer |
| 3f | T, E: the header's Back/Forward (app-navigation-and-zoom-controls.prd.md Phase 1) could walk the main frame off the app's own origin | Back and Forward call only the browser History API (`window.history.state`'s `idx`, `navigate(-1)`/`navigate(1)`) inside the webview's single origin; `useAppHistory`'s floor stops Back at the first in-app entry (or the one at a project switch, Q8) and its ceiling is a ceiling on in-app `idx` values only. Nothing pushes or replaces an external URL: every external link already opens in its own popup, never the main frame (Evidence, `3d`), so there is no off-origin entry for history to walk back or forward to | None: in-app history needs no allow-list, because there is nothing in it but the app's own pages. Owner: maintainer |

### 4. The Go host to the sidecars (`SECURITY.md` bullet 4)

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 4a | E, T: an option value from the UI becomes an option of the sidecar (`--mic --model-dir ...`) or a shell command | The sidecars are started with `exec.CommandContext` and an argv slice, never through a shell, inside a Windows Job Object (`internal/process/supervisor.go`, `internal/process/stream.go`); the engine is checked against the list this platform can launch (`teleprompter.Engines`: `whisper` everywhere, `moonshine` on Windows only) before anything starts, and the model directory is computed by the host from an installed, verified entry of that engine's own catalog (the UI's value is overwritten, `bindings.go` `liveModelDir`; the service also refuses `moonshine` with no directory); **a value that starts with `-` is refused by Python's `argparse` with exit status 2** ("expected one argument"), so a `--`-prefixed microphone, chapter, model, language or audio-file value cannot become another flag: verified 2026-09-21 and pinned by `sidecars/manuscript-teleprompter/tests/test_live_asr.py`; `--check-moonshine` takes no value and only the packaged smoke test passes it. The tail-audio locate (`--locate --wav --tail-start --tail-end`, [ADR 0111](../adr/0111-the-resume-point-comes-from-transcribing-the-recorded-tail-and-placing-it-with-the-tracker.md)) takes no value from the UI but a chapter id, a track GUID the host must find in the selected `.rpp`, and a model id checked against the catalog: the audio file and its range come from the `.rpp`, and `internal/teleprompter/locate.go` refuses, before any process starts, an audio path that is not absolute or not an existing regular file, a range that is not finite, is reversed or is longer than 120 s, and a model directory that is not absolute; the sidecar re-checks the range and requires `--model-dir` (`locate.check_args`, pinned by `tests/test_locate.py`), and the host accepts only a `locate` line whose word and sentence lie inside the chapter. Reading the credits ([ADR 0150](../adr/0150-the-teleprompter-reads-credits-as-a-host-rendered-script-file-not-a-chapter.md)) adds no UI-chosen value: the UI sends only the kind, checked against `opening`/`closing` (`creditsScript`, `creditsbindings.go`); the `--script-id`/`--script-title` values are host constants, and the rendered text is written by the host to a session-directory file (mode 0600, removed when the session ends) passed as `--script`, never as an argument. The pickup and duplicate scan (`TakeReviewScanStart`, take review Phase 5) passes the track name the narrator chose as `--track-name` in the same argv, and a `--progress` path the host builds in the session folder; the pickup track and time range never reach the sidecar: the host checks them (`takereview.ValidateScope`, and that the tracks are in the project) and uses them only to choose which items go into the manifest it writes (`apps/desktop/takereview_job.go`) | A hostile value fails the session and injects nothing. The values are still chosen by the narrator's own UI, and `--wav` (a fixture replay option, and the locate's input) is any file path the sidecar will read as audio: for the locate that is whatever file the selected `.rpp` names as a take's source, decoded by PyAV (FFmpeg), so a crafted project can point it at any audio or media file the narrator can read, and a malformed file reaches FFmpeg's demuxers. The output is one line checked by the host; nothing is written. A session that ends without its watcher (a host crash) can leave the credits text file in the session directory, the narrator's own text in their own profile. Owner: maintainer |
| 4b | T: the sidecar writes a JSON line that the UI then trusts | The host relays only lines that are valid JSON objects with a string `type` (`internal/teleprompter/service.go`; anything else is dropped and counted); the UI parses every event with a Zod schema, which decides what an event means, and drops and counts a wrong one ([ADR 0069](../adr/0069-payloads-are-validated-with-zod-behind-parsewire-and-a-wrong-shape-fails-loudly.md), [wire contracts](wire-contracts.md)); React escapes text and the UI never sets HTML | The sidecars are our own frozen programs, so a hostile line would already mean the release is compromised (row 8). Accepted |
| 4c | E: the embedded sidecars are extracted to a per-user folder and run from there | They are written at first launch into the per-user cache with a `.complete` marker keyed to the build (`apps/desktop/app.go`) | Another process of the same user can overwrite an extracted executable before it runs (no privilege is crossed). Accepted |
| 4d | D: a runaway sidecar | The Job Object kills the tree when the host exits; a stop file with an 8 s grace ends a live session ([ADR 0022](../adr/0022-live-sidecar-events-over-wails-and-stop-file.md)); jobs are cancellable | None worth a row |
| 4e | T, E: the recording-coverage manifest names the audio the sidecar reads and the words files it writes (`compare.py --coverage`, started by `apps/desktop/internal/coverage`, ADR 0128, and from the UI through `CoverageStart`, ADR 0129; see [the recording check](../utilities/recording-coverage.md)) | The UI's only input to a check is a chapter id (`CoverageStart(chapterId)`, `apps/desktop/bindings_coverage.go`), looked up in the imported manuscript and never used as a path; the model is the narrator's Transcript Compare setting, which must be in the approved Whisper catalog, and `--model-dir` is that model's verified install folder from the asset registry (an uninstalled model stops at the first-use gate before anything runs). The host builds every path from the project folder and the saved `.rpp` only, never from UI text: the `.rpp` is the Tracks page's selection and must sit directly in the project folder (`coverage.Service.savedProject`); the audio paths are the active takes' `<SOURCE FILE>` of the chapter's narrator-confirmed track, resolved by `tracks.Parse`; the manifest, `--words-dir`, `--out` and `--progress` are inside a per-run folder under `<project>/narration-utils/analysis/coverage/runs/`, removed when the run ends; `--manuscript` is the project's own `manuscript.json`; words files are named `w-<hash>.json` by the host and copied in from and out to the analysis evidence cache, whose entries are keyed by content hashes. The sidecar is started with an argv slice, never a shell (row 4a). On the sidecar side the manifest is JSON, checked field by field before any file is opened (types, finite numbers, unique indices, no unknown keys) and refused with exit 1 otherwise; a words file is a plain file name inside `--words-dir`, never a path (`[A-Za-z0-9][A-Za-z0-9._-]{0,199}`, so no separator, drive or `..`), written to a temporary file and renamed; a words file that does not parse is a cache miss, re-transcribed, never trusted in part; the chapter is chosen by id, with no name matching. The host reads the results file with a size bound and checks that its counts add up before it records anything; a region's `position`, `before` and `after` points ([ADR 0168](../adr/0168-a-coverage-region-carries-its-bounds-as-optional-before-and-after-points-with-no-version-bump.md)), and the word alignment's `COVERAGE_TOKEN` and `COVERAGE_EXTRA` lines (manuscript words, what was heard, an item index and source seconds; [ADR 0242](../adr/0242-the-recording-check-writes-the-chapters-word-alignment-as-additive-lines-and-align-again-never-transcribes.md)), are labels the host shows and stores, never a path and never used to open or slice audio; `--align-only` only narrows the mode (it transcribes nothing) and adds no input ([ADR 0127](../adr/0127-the-coverage-sidecar-mode-reads-a-json-manifest-keeps-one-words-file-per-item-and-writes-tagged-json-lines.md), [ADR 0128](../adr/0128-the-coverage-service-reads-the-saved-project-keeps-words-per-source-range-and-leaves-model-and-language-out-of-the-parameter-hash.md); `sidecars/transcript-compare/core/coverage_mode.py`, `apps/desktop/internal/coverage`, pinned by `tests/test_coverage_mode.py` and `internal/coverage/*_test.go`) | A source path in the saved `.rpp` can name any file the narrator's account can read (an absolute `FILE`), and the sidecar decodes it with PyAV: the same trust the narrator already gives REAPER's own project file. A words file in the cache is trusted to be the sidecar's own output (another process of the same user could replace it; no privilege is crossed). Owner: maintainer |
| 4f | T, E: the per-take divergence manifest names the audio the sidecar reads (`compare.py --take-divergence`, started by the take comparison job, `TakeComparisonStart`) | The manifest is JSON, checked field by field before any audio is opened (types, non-empty GUIDs and paths, finite non-negative offsets, positive lengths, whole sentence numbers in order, no unknown keys) and refused with exit 1 otherwise; the chapter is chosen by id, with no name matching, and the span must lie inside it; the only file written is `--out`, through a temporary file and a rename ([ADR 0141](../adr/0141-per-take-divergence-is-localized-by-the-markers-diff-and-asr-word-timestamps.md); `sidecars/transcript-compare/core/take_divergence_mode.py`, pinned by `tests/test_take_divergence_mode.py`). The host builds the manifest itself (`apps/desktop/internal/takecompare/compare.go`, `apps/desktop/takecompare_job.go`, [ADR 0165](../adr/0165-a-take-comparison-is-one-finding-per-group-over-its-one-span-built-from-the-saved-project-and-never-ranked.md)): the page sends only a take-review finding's id; the reads come from the finding the store holds, and each is re-resolved in the saved project by its item and take GUIDs, where its file, offset and length must still match, and the manifest names that take's own resolved source file; the chapter id is the manuscript's own; the manifest, results and progress files are in the host's session folder (`TestCompareWritesTheManifestFromTheSavedProject`, `TestTakeComparisonRunsAsAJobAndItsFindingIsListedForReview`) | The sidecar trusts the host for the audio paths (any file PyAV opens), and the host trusts the saved `.rpp` for them, as the pickup and duplicate scan (row 4a) and the `/media` route do: a hostile project file can point a take at any readable file, which the sidecar then decodes as audio and never writes. Owner: maintainer |

### 5. The REAPER file bridge (not in `SECURITY.md`; see the note under the table)

Boundary: the Go host and the Lua bridge in REAPER exchange files in one session folder ([the REAPER bridge](reaper-bridge.md), [ADR 0031](../adr/0031-reaper-integration-is-a-lua-file-bridge-verified-by-hand.md)).

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 5a | T, E: another process writes a command file, and the Lua then opens or writes a path the command names | The folder is `<REAPER resource path>\NarrationUtils\sessions\hub_<id>`, under the user's profile (on this machine `%APPDATA%\REAPER` inherits full control for the user, SYSTEM and Administrators only, read with `icacls` on 2026-09-21); the bridge dispatches only registered command names with a protocol version and an unknown one is an `ERROR` event ([ADR 0067](../adr/0067-bridge-commands-are-registered-by-name-and-each-feature-lives-in-its-own-lua-file.md)); a command is deleted after it is read; a command that changes the project writes one undo block and is safe to send twice | **No authentication.** The `0o600` mode the Go side writes is nearly meaningless on Windows (Go maps only the read-only bit); the protection is the profile's folder ACL, not the mode, and a portable REAPER installed elsewhere may not have one. Some commands **write** to a path taken from the command (`read_line_ids` opens its `path` for writing in `narration_line_identity.lua`, the compare commands read theirs, and `prepare_compare` creates `TranscriptCompare/diffs` under the project folder it is given, project-workspace PRD Phase 5), so a forged command can truncate or overwrite any file the user can. A same-user process can do that directly, so no boundary is crossed. Accepted; [#240](https://github.com/countrymanprime/narration-utils/issues/240) hardens the folder |
| 5b | I, T: the Lua bridge is untested code with file access | The Lua now has a harness: a fake `reaper` driven through the real file protocol, 300 tests and 105 mutation checks, run by `pnpm check` and CI on Linux and Windows under Lua 5.4 ([ADR 0066](../adr/0066-the-lua-bridge-is-tested-by-a-harness-under-lua-5-4-and-reaper-api-behaviour-is-checked-in-reaper.md)); a bridge bug was found and fixed by the scripted real-REAPER run | The harness proves the bridge's logic, not REAPER's: what an API does inside a real REAPER is checked by hand or by a scripted run on a copy of a project, and the owner steps are [#167](https://github.com/countrymanprime/narration-utils/issues/167). Owner: maintainer |
| 5c | R: events are not attributable | Every event and error carries the run ID of the command that caused it ([ADR 0068](../adr/0068-bridge-events-fan-out-to-subscribers-by-tag-and-run-and-every-error-names-its-run.md)) | None worth a row |
| 5d | T, D: a forged command moves REAPER: selects an item, moves the cursor, changes the time selection and repeat, or starts and stops playback (`navigate_item`, `loop_context`, `stop_loop`, review-dashboard Phase 6) | The same folder ACL as 5a; these commands take no path and open no file; they change selection and transport state only, never the project's content, and make no undo point ([ADR 0121](../adr/0121-going-to-and-looping-a-finding-is-by-guid-and-source-time-makes-no-undo-point-and-stop-restores-what-the-loop-changed.md)); a GUID that does not resolve is reported and never replaced by a neighbour; nothing moves while REAPER is recording, and Stop stops only a loop the bridge started and puts back only what that loop changed; each guard has a mutation check ([going to and looping a finding](reaper-navigation.md)) | A same-user process could start playback at the project's volume or change the narrator's time selection; it can already drive REAPER through its own actions, so no boundary is crossed. Accepted; #240 as for 5a. Owner: maintainer |
| 5f | T, E: the app itself moves REAPER when the narrator did not ask, or leaves a command in the folder that a REAPER started later acts on (the Review page's Go to, Loop and Stop, review-dashboard Phase 7) | The host sends `navigate_item`, `loop_context` and `stop_loop` only from the `FindingsGoTo`, `FindingsLoop` and `FindingsStopLoop` bindings, which the page calls only on a click; `FindingsReaperStatus`, which the page polls, reads the heartbeat REAPER already sends and sends nothing. A binding refuses before writing a command when there is no bridge or REAPER's heartbeat is stale, and the page disables the buttons with the reason; a finding without an item GUID is refused before anything is sent. The UI sends only a finding's id (and, for one read of a take-review group, `FindingsGoToRead`/`FindingsLoopRead`, the read's index): the host reads the GUIDs and source times from the project's own findings store, so the page cannot name an arbitrary item, and an index the finding does not have is refused before anything is sent (`apps/desktop/bindings_navigation.go`, tested in `bindings_navigation_test.go` and `bindings_navigation_reads_test.go`) | A command written in the moment between the last heartbeat and REAPER closing stays in that session's folder; the next REAPER launch opens a new session folder, so nothing acts on it. None worth an issue. Owner: maintainer |
| 5g | T, E: the app or a forged command changes the project: the Review page's approved marker adds a take marker (`add_finding_marker`, `FindingsAddMarker`, review-dashboard Phase 8) | The host sends `add_finding_marker` only from `FindingsAddMarker`, which the page calls only after the narrator confirms in a dialog, and refuses before writing a command when the finding is not accepted, has no item GUID or source time, or REAPER is not listening (as 5f); the UI sends only the id, and the host builds the marker's name and colour from the store and the Settings, so the page cannot choose an item or text. In REAPER the command takes no path and opens no file, finds the take by GUID and never uses a neighbour, refuses while recording, adds at most one marker (a marker of the same kind within 0.15 s is answered `existing` and nothing changes) and wraps the add in one undo block; each guard has a mutation check (`apps/desktop/bindings_marker.go`, `integrations/reaper/narration_navigation.lua`, [ADR 0123](../adr/0123-the-approved-marker-is-one-take-marker-for-an-accepted-finding-named-like-transcript-compares-in-one-undo-point.md)) | A forged command (5a) could add take markers with any name to any item whose GUID it knows; they are visible, and one Undo each removes them, and a same-user process can already edit the project through REAPER. That REAPER's own undo takes the marker away in one step is checked by the harness's fake only; the check in a real REAPER is pending ([reaper-navigation.md](reaper-navigation.md#manual-verification-checklist) steps 10 and 11). Accepted; #240 as for 5a. Owner: maintainer |
| 5h | E, T: a command makes REAPER run an action or load code (render, apply FX, a script) | Only `launch_cleanup_tool` calls `Main_OnCommand`, and only for a key in its allow-list (`repair_pops_clicks`, `magnolius_declick`); the host sends the key, never an action ID or name, and both `narration_cleanup.lua` and `apps/desktop/internal/cleanuptools` refuse any other key before anything runs. The action is found by its exact action-list name, never under the narrator's `Script:`/`Custom:` names for a native tool, and an ambiguous match runs nothing; the launch opens a dialog and changes nothing itself ([ADR 0146](../adr/0146-cleanup-launchers-open-an-allow-listed-reaper-action-found-by-its-name-and-change-nothing-themselves.md); `cleanup_test.lua` and five mutation checks). `apply_fx_chain` and `add_take_fx` (S28, experimental; [ADR 0234](../adr/0234-fx-chains-go-on-tracks-and-a-passage-of-a-take-gets-one-plug-in-at-a-time.md)) load effects: a chain goes only on a track or the master track, named only by its path relative to REAPER's own `FXChains` folder, which must be one `list_fx_chains` lists and must exist, with the full path built on the Lua side from REAPER's resource path, so no `..`, drive, absolute path or backslash from a command reaches the file system (the Go client refuses such a name before sending); a passage of a take gets exactly one plug-in per request, named exactly as `EnumInstalledFX` lists it (never a chain file, a path or the FX container), on one split-out piece of the item it names by GUID; each is one undo block (`workspace_test.lua` and its mutation checks). None of them calls `Main_OnCommand`, and the host refuses `apply_fx_chain` and `add_take_fx` while the Experimental REAPER actions setting is off | A same-user process that can write the command folder can open the Repair Pops/Clicks dialog or the narrator's installed Magnolius DeClick on the current selection; neither applies anything without the narrator. A narrator who installs a *different* script named `Magnolius_DeClick.lua` gets that script run by the button: the app trusts the name, not the file, the way REAPER's own action list does. The same process can add any chain the narrator already keeps in `FXChains` to a track, or any installed plug-in to an item whose GUID it knows: that code runs inside REAPER, as it would from the narrator's own FX browser, and one Undo removes it. Whether REAPER follows a junction inside `FXChains` is pending the verification pass (row A7). Accepted |
| 5i | T: a command changes what the narrator hears in REAPER (which retake lane plays) | Only `pick_retake_lane` changes lane play state, and only one value: the track `C_LANEPLAYS:<lane>` of the item the command names, set to 1 in one undo block, so one Undo in REAPER restores what played. It never turns lanes on or off, converts takes and lanes, moves items or runs an action, and it refuses an item that is gone, no longer carries the named line id, or sits on a track not in fixed-lane mode; the lane comes from the item in REAPER, never from the command. The host sends it only for a retake it lists from the saved project ([ADR 0147](../adr/0147-retakes-on-fixed-lanes-are-chosen-by-lane-play-state-and-the-app-never-converts-takes-and-lanes.md); `retake_lanes_test.lua` and seven mutation checks) | A same-user process that can write the command folder can silence every lane but one on a lane track by naming an item GUID and its line id; the change is one visible undo step and deletes nothing. What REAPER does with the value is read back by spike S7, not yet by a scripted run of this command or by ear ([pending](../research/reaper-retake-lanes.md)). Accepted |
| 5j | I: a read-only command reports the transport and a track (`chapter_track_state`, S28): play state, cursor, the saved project path, which tracks are armed, the record input, REAPER's input device name, and the track's items with their source file paths | It sets nothing and opens no undo block; it takes no path and opens no file; a track is named by GUID and a GUID that does not resolve answers `TRACK_STALE`, never another track; answers go to `events.log` in the session folder of 5a, next to `prepare_compare`'s answers, which already carry the same kind of paths. The host refuses to send it while the Experimental REAPER actions setting is off, and it is experimental until the verification pass confirms it ([ADR 0230](../adr/0230-reaper-commands-built-before-the-verification-pass-are-refused-by-the-host-while-the-experimental-switch-is-off.md); `track_state_test.lua` and five mutation checks) | A same-user process that can write the command folder can read the project's audio file paths and the audio device's name from `events.log`; it can read the `.rpp` directly, so no boundary is crossed. Accepted; #240 as for 5a. Owner: maintainer |
| 5k | T, E: a forged or unasked command starts or stops recording in REAPER, or changes which tracks are armed (`arm_only`, `record_start`, `record_stop`, S28) | Registered command names only (5a's folder ACL); the track is named by GUID and a GUID that does not resolve changes nothing; `record_start` records only when REAPER is stopped and the named track is the one armed track, and presses record once; `record_stop` stops only a recording this bridge started (`RECORD_NOT_OURS` otherwise), so a recording the narrator started is never touched; `arm_only` is refused while REAPER records and remembers the narrator's arms, which come back when the app's recording stops, except an arm the narrator changed meanwhile; none calls `Main_OnCommand`. The host refuses all three while the Experimental REAPER actions setting is off, no binding sends them yet, and the control bar will send them only from a click with the track GUID from the host's mapping ([ADR 0232](../adr/0232-arming-for-a-recording-remembers-the-narrators-arms-makes-no-undo-point-and-the-app-stops-only-the-recording-it-started.md); `transport_test.lua` and eleven mutation checks) | A same-user process that can write the command folder can arm a track and start a recording on it (REAPER writes a new media file under the project's record path) or stop the app's own recording; the narrator sees the transport and every take is an ordinary REAPER item. Whether a track arm is undoable, and what REAPER's own record mode and save prompt do, is pending the verification pass (rows A4, A5, B2 to B6). Accepted; #240 as for 5a. Owner: maintainer |
| 5l | T: a command changes which take an item plays, or adds or moves regions (`set_active_take`, `create_regions`, S28) | Both find what they change by GUID or exact title and never fall back to a neighbour; `set_active_take` changes one item's active take in one undo block and nothing while REAPER records ([ADR 0233](../adr/0233-the-app-may-make-a-take-active-by-guid-in-one-undo-step.md)); `create_regions` reads its rows from a payload file in the session folder (the path is the command's, as 5a says of every payload), adds only regions that do not exist, moves one only when the host asks and exactly one region holds that title, and does it in one undo block ([ADR 0235](../adr/0235-one-create-regions-command-serves-chapter-and-credits-regions-and-replaces-create-chapter-regions.md)). The host builds those rows itself from the saved `.rpp` and the narrator's confirmed chapter links, never from bounds the UI sends ([ADR 0245](../adr/0245-chapter-and-credits-regions-are-planned-by-the-host-from-confirmed-links-and-the-saved-project.md)). The host refuses both while the Experimental REAPER actions setting is off (`workspace_test.lua`, `regions_test.lua` and their mutation checks) | A same-user process that can write the command folder can switch an item's active take or add and move regions; each is one visible undo step and deletes nothing. Accepted; #240 as for 5a. Owner: maintainer |
| 5m | T, I: the punch commands read the transport and move the edit cursor (`play_position`, `punch_to`; teleprompter PRD Phase 12) | `play_position` sets nothing and takes no path; `punch_to` takes two numbers, refuses anything but a finite time and a pre-roll of 0 to 10 s, moves only the edit cursor (no undo block, never playback) and refuses while REAPER records. The host refuses both while the Experimental REAPER actions setting is off ([ADR 0246](../adr/0246-punch-and-roll-moves-only-the-edit-cursor-and-anchors-words-by-a-polled-play-position.md); `punch_test.lua` and six mutation checks) | A same-user process that can write the command folder can read the play position or move the edit cursor; neither changes the project. Accepted; #240 as for 5a. Owner: maintainer |

`SECURITY.md` lists what a reporter should look at; the bridge is a boundary a reporter might not think of, so its scope bullet is added there ("the file protocol between the app and REAPER, and the paths the Lua opens from a command").

### 6. Opening a project or a manuscript (`SECURITY.md` bullet 4)

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 6a | T, D: a hostile DOCX (zip bomb, path tricks) | The DOCX is read with `zip.OpenReader` and only named entries are read into memory; nothing is extracted to disk, so there is no zip-slip (`internal/importer/docx.go`); PDF import is behind the `pdf_candidate` build tag and is not in a shipped build. `credits.Detect` (credits-token-setup-and-front-matter-detection.prd.md) re-reads the project's own stored copy of the source a second time, for title/author suggestions: `storedSourcePath` (`internal/credits/suggestions.go`) confines the read to `<project>/narration-utils/manuscript/sources/`, refusing a `manuscript.json` `storedPath` that tries to escape it via `..` or an absolute path, so a hand-edited or hostile manifest cannot steer this second read anywhere else on disk; `ReadEPUBMetadata`'s OPF read goes through the same capped `epubEntry` reader as import (row 6d) | DOCX docProps/core.xml is read the same uncapped way as the rest of the DOCX (`io.ReadAll`, [#239](https://github.com/countrymanprime/narration-utils/issues/239)): a small file that inflates to gigabytes can exhaust memory. Owner: maintainer |
| 6b | I, E: the `/media` route serves an arbitrary local file | It serves only a path that equals a source file of the current project's parsed `.rpp`, re-resolved on every request, and opens the project's own copy of the path, never the requested string (`apps/desktop/media.go`, [ADR 0012](../adr/0012-media-route-for-track-playback.md)); it is reachable only from the app's own webview | A hostile `.rpp` can list any local file as an item source and the app's webview would then play it (audio decoders only; the response goes nowhere). Accepted |
| 6c | E: a project path that is not a project | `ProjectCreate` requires an absolute path and refuses while the host is busy; `ProjectSwitch` takes any path the narrator chose in the picker (`apps/desktop/bindings.go`) | Project data is written under the folder's own `narration-utils/` folder; the `.rpp` and the audio are read wherever they are. `ProjectSwitch` does no check beyond that the narrator chose the folder. Accepted |
| 6d | T: a hostile `.rpp`, manuscript or audio parsed by native code | Parsers are Go (memory safe) or Python; the WAV reader is bounded by its header checks; `go test -fuzz` and Hypothesis property tests run in the gate ([ADR 0044](../adr/0044-property-and-fuzz-tests-are-deterministic-in-the-gate.md)), including the `.rpp` region reader and the chapter-to-track matcher over track and region names (`FuzzParseRegions`, `FuzzFindChapterByTrackName`); the chapter suggestion ([ADR 0113](../adr/0113-the-teleprompter-suggests-a-chapter-from-the-saved-armed-track-and-preselects-only-a-confident-match.md)) reads two more track flags (`SEL`, `REC`) from the same parse and only preselects a picker value; chapter sync's watcher ([ADR 0210](../adr/0210-chapter-sync-watches-the-saved-rpp-and-reapers-edit-counter-and-sends-reaper-nothing.md)) re-parses the linked `.rpp` on its own after each save, but only while the narrator has turned sync on, at most once per 1.5 s settle, with the same parser, and a sync only adds links the store accepts (never over a manual one, ADR 0202) | A native decoder (`av`, `onnxruntime`) has its own bugs. Pinned and scanned. Owner: maintainer |
| 6e | E: the second launch chooses the program a sidecar runs | The second launch re-parses launcher arguments including the `--*-python` and `--repo-root` overrides (`onSecondInstance`, `parseConfigArgs` in `apps/desktop/app.go`); a single-instance lock forwards it to the first instance | Any process of the same user that can start the executable can point a sidecar at another program. No privilege is crossed. Accepted |
| 6f | T, S: the `--daw` argument chooses which DAW the host talks to | It is kept only as a display label (React renders it as text); `dawadapter.Classify` is the one reader, matching `REAPER` or `Audacity` case-insensitively, and any other value selects nothing. An Audacity launch never opens the REAPER file bridge, even with a `--session-dir`, and its review adapter refuses every request until the pipe client exists ([ADR 0144](../adr/0144-a-launch-names-its-daw-with-daw-and-an-audacity-launch-opens-no-reaper-bridge.md), `apps/desktop/audacity_test.go`). The installer's "Narration Utils for Audacity" entry passes the constant `--daw Audacity` and nothing else, and a picker switch keeps only that label, never a REAPER one ([ADR 0145](../adr/0145-the-audacity-launcher-is-an-installer-start-menu-entry-and-a-picker-switch-keeps-an-audacity-launch.md)) | A same-user process can relabel a launch or switch the bridge off by passing `--daw Audacity`; it could already choose the session folder and the sidecars (6e). No privilege is crossed. Accepted |
| 6g | I, T, D: the page names the audio files the host measures or checks (`MeasureAnalyze`, diagnostics PRD Phase 1; `DiagnosticsAnalyze`, Phase 6) | Both read only the same allowlist (`measurePaths`; `DiagnosticsAnalyze` also refuses a source kind that is not `raw_recording` or `processed_render`, `apps/desktop/diagnostics_job.go`, and saves no finding). The host measures only an absolute path the narrator chose in the operating system's picker (`MeasurePickFiles`) in this app session; any other path, a relative one, an empty list or more than 500 files is refused before a file is opened ([ADR 0156](../adr/0156-measurement-reads-only-files-picked-this-session-as-one-job-and-fingerprints-the-bytes-it-read.md), `apps/desktop/measure_job.go`). Files are opened read-only and parsed by the Go WAV reader (row 6d: bounded by its header checks, mono and stereo PCM or float only, anything else refused with its reason); nothing is written and the paths are not logged. One measurement runs at a time and can be cancelled mid-file | A picked file of any size is read to its end (for the SHA-256), so a huge pick takes long; it is cancellable and the narrator chose it. The answer carries the full path back to the page, which already had it. Accepted |
| 6h | T, I: the app writes a report file for the narrator to share (`DeliveryExportReport`, diagnostics PRD Phase 7), which could land somewhere it should not, overwrite something, or leak where the narrator keeps files | The page sends one boolean (include full paths) and nothing else: the host builds the report from the jobs, settings, review store and asset list it already holds, and writes it only under the open project's `narration-utils/delivery` folder, under a name made from the time (`delivery-report-<UTC stamp>[-n]`), each file through a temporary file and a rename, never over an existing report and never next to the audio; with no project it refuses and writes nothing (`apps/desktop/delivery_report.go`). By default no path is written: each file is named by its base name, a known path or its folder inside a message or evidence becomes the file name or `[folder]`, and any other drive, UNC or home path is removed; no audio, audio reference or manuscript text is ever written (`apps/desktop/internal/deliveryreport/redact.go`, tested with paths in error messages, evidence and asset folders). The HTML is rendered by `html/template` (every value escaped, a file named `<script>` is text), has no script and fetches nothing. The audio's SHA-256 is unchanged across an export (`delivery_report_test.go`) | A file name the narrator chose (a chapter file named after a person, say) is written as is, since it is what identifies the file to the recipient. Ticking "include each file's full location" writes the paths, user name included, by the narrator's choice. Accepted |
| 6i | T: the app reads back a file of the narrator's delivery profiles (`delivery-profiles.json`, beside `credit-templates.json`; [ADR 0180](../adr/0180-custom-delivery-profiles-are-copies-of-a-built-in-kept-in-a-user-level-file.md)) and the project's choice of profile (`project.json`), and a hand-edited or hostile file changes how the Delivery page and the report judge the files | The built-in ACX profile is compiled in and never written. The file is read as narrator data through `persist` ([ADR 0069](../adr/0069-payloads-are-validated-with-zod-behind-parsewire-and-a-wrong-shape-fails-loudly.md), rule 8) and validated before use (`internal/deliveryprofile/store.go`): a schema version it knows, at most 100 profiles, every rule naming a metric the app knows, finite bounds, a lowest not above its highest; a file that fails is kept aside and reported, and the built-ins still judge. A choice naming a profile that does not exist falls back to the Global default with a notice. Saving goes through `DeliverySaveProfile`, which accepts at most 64 KiB, changes only names, bounds and on/off of rules the copy already has, and writes through a temporary file and a rename. The file only ever changes a verdict: nothing it holds is executed, opened, fetched or written elsewhere | Someone who can write the narrator's profile folder can make a file read as "met" when it is not. They could already edit the audio and the settings. Accepted |
| 6j | T, D: a hostile MP3 the narrator picks for measuring is parsed in the host (delivery profiles PRD Phase 6; `internal/measure/mp3header.go`, [ADR 0237](../adr/0237-an-mp3-is-measured-for-its-container-only-from-bounded-frame-headers-and-its-levels-are-not-checked.md)) | It is read only if it was picked in this session (row 6g) and its first bytes are an ID3v2 tag or a frame sync. Nothing is decoded: Go (memory safe) reads the ID3v2 size (syncsafe, else refused), then 4-byte frame headers, each frame's length computed from its own header and read through a 64 KiB buffer, so no read or allocation depends on a length beyond one frame (at most 1441 bytes). A frame found by searching counts only if another frame of the same stream follows it. The search gives up after 64 KiB without a frame, or after 1 MiB of skipped bytes in the whole file, with a named reason. Free-format, reserved and non-Layer III headers are refused. Cancel is checked every 2048 frames. `go test -fuzz FuzzReadMP3` (seeded with tagged, VBR and truncated files) asserts that the reader answers or refuses and that what it answers holds together | A long MP3 is read to its end, header by header, and then hashed in full (as for a WAV, row 6g); it can be cancelled. A crafted file can be read as a valid MP3 with the numbers its headers claim, which changes a verdict only (as in row 6i). Accepted |
| 6k | T, E: the app writes and reads back where the prompter last was in each chapter (`<project>/narration-utils/teleprompter/<chapter>.reading.json`, read-aloud-resume-from-daw PRD Phase 2, [ADR 0205](../adr/0205-the-prompter-remembers-its-last-word-per-chapter-in-a-host-named-file-dropped-when-the-text-changes.md)), and a hostile or stale file could name a path or move the resume point | Nothing from the UI reaches it: the host writes it when a chapter session ends, from the chapter id it launched and the sidecar's last `position` event (`internal/teleprompter/reading.go`). The chapter id must be a chapter of the imported manuscript before it names a file, and the file name is built by the host: the id itself only when it matches `[A-Za-z0-9_-]{1,64}`, otherwise a hash of it, so no id becomes a path (`TestAChapterIDIsNeverUsedAsAPath`). The write goes through a temporary file and a rename. On read, a file that does not parse, has another version, names another chapter, has a `read` outside `[0, tokens]`, or whose `scriptHash` (the document id and the chapter's text) no longer matches the manuscript reads as absent. Credits sessions write nothing | Another process of the same user can write a reading that passes these checks. The worst it can do is suggest a start word inside the chapter, which the narrator sees and can change (Phase 3). Owner: maintainer |

### 7. Data at rest (`SECURITY.md` bullet 1)

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 7a | I: another local user reads manuscripts, audio or the Story Bible | Data lives in the user's profile and the project's own folder; the host log is capped (1 MiB plus one backup) and records where, not what | Not encrypted, by design: it is local-first software and the operating system's account boundary is the protection. Accepted |
| 7b | I: content leaves the machine by an analysis or a sidecar | No code path uploads content; the network fence in [What the app sends off the machine](#what-the-app-sends-off-the-machine) and the offline start-up test | A sidecar's own network use is not seen by the Go fence; the sidecars import no HTTP client of their own (checked 2026-09-21: only `urllib.parse` in `libs/python/narration_common/ui_bridge.py`), and the only self-downloading paths are the developer-run ones named above. Owner: maintainer |

### 8. The release pipeline (`SECURITY.md` bullet 5)

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 8a | T, S: a malicious dependency or workflow change reaches an installer | Promote never rebuilds: it re-attaches the candidate's bytes; every asset carries a SHA-256 file and build provenance, and promote verifies both before it creates the stable tag ([CI and releases](../operations/ci-and-releases.md#build-provenance)); every action is pinned to a commit and `zizmor` blocks a workflow that is not; least-privilege `permissions` and no stored checkout credential ([workflow security](../operations/ci-and-releases.md#workflow-security)); the Windows release is built in a job with no write permission and published by a separate job that checks the built files against the digests the build recorded before it attests them; the only cached build output, the sidecar freeze, is keyed on a hash of every input, saved only from `main` (a pull request can restore it, never write it), and still smoke-tested on every build ([the sidecar freeze cache](../operations/ci-and-releases.md#the-sidecar-freeze-cache)); the `production` environment needs the owner's approval; Dependabot with cooldowns; advisory CodeQL, dependency review (a licence allow-list and a high-severity gate), `govulncheck` and OSV-Scanner | **Unsigned** (D7): SmartScreen warns and provenance shows which workflow built a file, not that its source is benign. No required status check exists (D11), so the maintainer's review is the gate; the Prerelease does not re-run the quality jobs, so a pull request merged with red checks becomes a release candidate ([#544](https://github.com/countrymanprime/narration-utils/issues/544)). No SBOM. Owner: maintainer |
| 8b | T: an installer or the update zip differs from the reviewed build | The setup program is built in the same job and attested: `scripts/release/wails-build.mjs --installer` renders `wails_tools.nsh` for the release version and runs `makensis` on the checked-in `project.nsi`, and embeds the WebView2 bootstrapper that the pinned `wails3` CLI carries (no download while building; `build-native` checks its Microsoft signature) ([ADR 0200](../adr/0200-the-desktop-shell-runs-on-wails-v3-beta-pinned-at-v3-0-0-beta-25.md)); the update zip holds exactly one file ([ADR 0082](../adr/0082-windows-installs-per-user-from-an-nsis-setup-program-that-wails-builds-and-the-release-carries-beside-the-update-zip.md)); the packaged smoke test runs the frozen program before it ships. The installer adds two Start Menu entries: the app, and "Narration Utils for Audacity" with the constant argument `--daw Audacity` ([ADR 0145](../adr/0145-the-audacity-launcher-is-an-installer-start-menu-entry-and-a-picker-switch-keeps-an-audacity-launch.md)). `scripts/release/installer.test.mjs` pins both Start Menu entries' targets and arguments, and the uninstaller deletes both. No Audacity-side file (a macro or a plug-in) ships | An owner step, the first attested release, is [#188](https://github.com/countrymanprime/narration-utils/issues/188) |
| 8c | I: licence obligations are not met, so a release cannot be used freely | The project is AGPL-3.0-or-later ([ADR 0039](../adr/0039-the-project-is-licensed-agpl-3-or-later.md)); the notices (third-party licences and the AGPL text with a source offer) are a release asset and are in the zip; model licences are recorded per artifact | The notices are generated from what the release contains and carry the AGPL text and a source offer ([third-party notices](../operations/ci-and-releases.md#third-party-notices)); every model has a recorded licence and commercial-use finding, and an unclear one stays out of the catalog ([model provenance](model-provenance.md)). `moonshine-voice` (MIT, re-read at the pinned wheel) ships two prebuilt DLLs, `moonshine.dll` and its own ONNX Runtime 1.23 (MIT, a manual entry in the notices); both are pinned by the wheel's hash in `uv.lock`, and the smoke test loads them. Not verified: whether the FFmpeg build inside the PyAV wheel is LGPL or GPL, and libx265's licence (recorded in the notices' manual entries). Owner: maintainer |

### 9. DAW detection and the download-page open (not in `SECURITY.md`; new boundary)

Boundary: the Go host reads Windows registry keys and well-known filesystem paths to detect a third-party DAW, and opens a hardcoded, compile-time URL in the narrator's default browser ([the DAW catalog and "Get it" flow](daw-integration.md#the-daw-catalog-and-get-it-flow); `apps/desktop/internal/dawcatalog`, `apps/desktop/dawcatalog.go`). The app never downloads, verifies, bundles or executes a DAW installer.

| # | Threat | Existing mitigation | Residual, owner |
| --- | --- | --- | --- |
| 9a | I: reading the registry/filesystem to detect REAPER discloses more than "installed or not" | Detection reuses `apps/desktop/internal/daw.LocateReaperExecutable` (the same probe `project-workspace-and-daw-link` Phase 6/[ADR 0092](../adr/0092-reaper-executable-discovery-heartbeat-mechanism-and-script-plus-project-launch-are-resolved.md) built and verified), scoped to the documented uninstall-registry key and well-known install path only; the result crosses the Wails boundary as `installed`/`path`/`source`, read-only, and gates nothing (Open Question A2) | None worth a row: the probe is narrow and its result is already shown to the narrator elsewhere (Settings > DAW Integration's launcher fields) |
| 9b | S: `DawCatalogOpenDownloadPage` opens a URL an attacker chose | The id crosses the Wails boundary, never a URL; the host resolves it server-side against the hardcoded `dawcatalog.Catalog` literal (`dawcatalog.Lookup`) and refuses an unknown id; the URL is a Go string constant, never fetched, loaded from a file, or built from narrator or network input (mirrors `update.go`'s `openReleaseNotes`, the same discipline for the same reason) | Spoofing risk is nil because the destination is a compile-time constant reviewed in code, not derived from anything the narrator or a remote response supplies. A vendor changing their own download URL would need a one-line code change here, not an attack; A4's recommendation stays a manual check |
| 9c | E: opening a browser tab is used to run or replace something else | `openInBrowser` (`apps/desktop/wailsapp.go`) calls Wails v3's `Browser.OpenURL`, which on Windows starts `rundll32 url.dll,FileProtocolHandler <url>` from an argv slice, never a shell (as v2's `runtime.BrowserOpenURL` did), so it hands the URL to the OS's default-browser handler; it performs no HTTP request itself, so it does not add a fourth import to the network fence in [What the app sends off the machine](#what-the-app-sends-off-the-machine) | The narrator's own default browser and whatever it does with a `reaper.fm` URL is outside the app's control, same as clicking any link. Accepted |

## Residual risks the owner has accepted or must decide

| Risk | State |
| --- | --- |
| The release is unsigned and the app does not verify provenance when it updates itself | Accepted for the first stable (D7); tracked in [#199](https://github.com/countrymanprime/narration-utils/issues/199) |
| No Content-Security-Policy in the webview | Open (row 3b); Google Fonts on every launch was fixed in [#238](https://github.com/countrymanprime/narration-utils/issues/238) |
| DOCX entries have no size limit | Finding: [#239](https://github.com/countrymanprime/narration-utils/issues/239) |
| The bridge folder relies on the profile's ACL, and `0o600` does nothing on Windows | Finding: [#240](https://github.com/countrymanprime/narration-utils/issues/240) |
| Same-user processes can alter what the same user's program runs (staged updates, extracted sidecars, the second launch, the bridge) | Accepted: no boundary is crossed |

## What this model does not cover

macOS and Linux preview builds (they are not supported and not installed by the update); the developer environment and the `uv` environment that downloads Python packages (developer tooling, not a shipped surface); the web pages of GitHub and Hugging Face; a narrator's own REAPER setup; physical access to an unlocked machine.
