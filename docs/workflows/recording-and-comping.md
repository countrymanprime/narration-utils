# Workflow: Recording, Pickups, and Comping

## Goal

Turn retakes and repeated reads into organized comparison candidates without making automatic editorial decisions.

## Flow

What each step runs on is in [Take review](../utilities/take-review.md); the screen-by-screen guide is the Review page's
[Pickups and duplicates](../guides/using-the-app/review.md#pickups-and-duplicates) section.

1. Record normal narration and pickups: as takes of the timeline item, on a pickup track, or later in the timeline.
2. On the Review page, **Find pickups and duplicates…** on the chapter track, adding the pickup track or stretch of the
   timeline if there is one. Every take of every item in scope is transcribed and aligned to the manuscript.
3. Review each group of repeated reads: its part of the script, each read's range, whether it covers the whole part or
   only some of it, and how closely it matches. Go to or loop a read in REAPER, or audition two from their raw source.
   Dismiss a line repeated on purpose.
4. Accept a real alternate, then **Add as take…**: choose the target item and the read that becomes the take. REAPER adds
   it in one undo step; the active take and the item's length stay as they were.
5. **Compare takes…** sets the reads side by side over the same words: how each read the script, word by word, and its
   clipping, room noise, level, length and pauses, each measured or unavailable with its reason. Nothing is ranked.
6. Choose the active take in REAPER yourself.

## Safeguards

- Do not treat repeated prose, echoed dialogue, or deliberate rehearsal as duplicates without a confidence threshold and context.
- Never insert, move, delete, or activate a take automatically.
- Do not compare takes with mismatched manuscript spans as though they were alternatives.

## Success signals

- A likely pickup is found faster than manually hunting through a session.
- A narrator can audition alternatives in context and explain why one was selected, from evidence that is never collapsed
  into one score.
