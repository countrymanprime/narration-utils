# Design system reference

**Status: living document — describes current state, not a point-in-time decision.** Each entry links back to the ADR that justified it. Unlike `docs/adr/`, this file is meant to be edited as the system evolves; the ADRs underneath it are what stay immutable.

## Tokens

Design tokens are CSS custom properties defined in `apps/ui/src/styles.css`'s `:root` block, with dark-mode overrides in a single `:root[data-theme='dark']` block. New Tailwind-based components reference them via arbitrary-value syntax — `bg-[var(--surface)]`, `border-[var(--border)]`, `text-[var(--text-muted)]` — rather than duplicating values into `tailwind.config.js`. See [ADR 0003](../adr/0003-tailwind-tokenized-primitives.md).

Key tokens: `--bg`, `--surface`, `--surface-2`, `--surface-3`, `--border`, `--text`, `--text-muted`, `--non-text`, `--accent`, `--accent-strong`, `--accent-soft`, `--accent-contrast`, `--danger`, `--character`/`--place`/`--org`/`--review` (category colors), `--backdrop` (modal/drawer scrim), `--shadow`, `--shadow-lg`. `--space-*`/`--font-size-*` are additive spacing/type-size steps, grounded in values already repeated across the file — not a full scale, for new/touched code to converge on rather than picking another one-off rem value.

**Colour meets WCAG AA** ([ADR 0059](../adr/0059-text-colours-meet-wcag-aa-with-two-text-levels-a-non-text-token-and-derived-on-tint-text.md); the rules, the token table, how to choose a colour and how the guard works are in [colour-and-contrast.md](colour-and-contrast.md)): there are two text levels, `--text` and `--text-muted`, and one colour for what is seen and not read, `--non-text` (icons, dots, glyphs). `--text-faint` no longer exists. Status text uses `--danger-text`, `--warn-text` and `--info-text` (`--danger`, `--warn` and `--info` stay for borders, dots and fills); the current navigation item is `--accent-strong`; highlights and entity badges draw `--<kind>-text` on a tint of the kind colour, and the eight kind colours have dark values. Placeholders are `--text-muted`. `apps/ui/src/paletteContrast.test.ts` holds every declared pair to 4.5:1 (text) or 3:1 (marks) in both themes.

**Theme switching:** `data-theme` on `<html>` is set by `ThemeProvider`/`useTheme` (`apps/ui/src/theme/`), a tri-state Light/Dark/System preference persisted to `localStorage`, exposed via a new "Appearance" category in `Settings.tsx`. An inline bootstrap script in `index.html` sets the attribute before first paint to avoid a flash of the wrong theme. See [ADR 0010](../adr/0010-theme-switching.md).

## Primitive components

Location: `apps/ui/src/components/primitives/`.

**The component atlas is the source of truth for what each primitive can look like.** Every primitive has a `<Name>.stories.tsx` next to it (one story per variant/state, `play()` for the primary interaction). Browse it with `pnpm --dir apps/ui storybook`; `pnpm --dir apps/ui atlas` builds it and captures every story in light/dark at a wide and a narrow viewport, failing on axe violations, a throwing `play()`, sideways overflow, or console errors. `src/atlasCoverage.test.ts` fails if a primitive has neither a story nor a recorded exemption, and `src/stories.test.tsx` runs every story as a unit test. The table below is a summary; when it and the atlas disagree, the atlas wins. See [ADR 0023](../adr/0023-visual-suite-capture-contract-and-storybook.md).

| Component | Purpose | Notes |
| --- | --- | --- |
| `Dialog` | Modal shell on Base UI (backdrop, head, scrollable body, optional footer, action row) | Max-width `70vw`, `overflow-x-hidden` + `break-words` body ([ADR 0001](../adr/0001-import-dialog-max-width-and-overflow.md)); `actionsAlign="between"` default, `"end"` for single-action dialogs ([ADR 0002](../adr/0002-dialog-action-button-placement.md)). Really modal: the page behind is hidden and unreachable, Tab loops, focus returns to the opener. Escape closes when there is an `onClose` (or `onEscape`); the backdrop never does; focus starts on the body unless a child has `autoFocus` ([ADR 0048](../adr/0048-every-dialog-is-one-modal-shell-and-confirms-are-alert-dialogs.md)). `footer` is a `flex-none` region between the body and `actions` that never scrolls, with no layout opinion of its own - the read-aloud dialog's control bar ([ADR 0196](../adr/0196-the-read-aloud-control-bar-lives-in-a-dialog-footer-slot-and-space-plays-or-stops-a-session.md)) |
| `ConfirmDialog` | Question with Cancel and a confirm (`Dialog` as an `alertdialog`) | `body` is its accessible description; `confirmVariant="danger"` (red) for actions that destroy data; a third `danger` action needs its `dangerLabel` |
| `WorkDialog` | Progress dialog for a background job | `ProgressBar` (Base UI `Progress`, `aria-valuenow` only when determinate), message in a live region; Escape works only once the job has finished. A running job with no Cancel stays blocking and says "This step cannot be cancelled. Close appears when it finishes." as the dialog's description, and draws no action row (`Dialog` draws its action row only when `actions` is not `null`); the fill eases and slides only under `motion-safe:` ([ADR 0057](../adr/0057-a-running-job-that-cannot-be-cancelled-keeps-its-dialog-blocking-and-says-so.md)) |
| `TooltipTarget`, `Tooltip` | Hints on Base UI ([ADR 0049](../adr/0049-hints-are-base-ui-tooltips-that-meet-wcag-1-4-13-and-info-icons-are-buttons.md)) | `TooltipTarget` wraps a control that names itself; the hint shows after 1000 ms of hover or at once on keyboard focus, stays while hovered, and Escape dismisses it (WCAG 1.4.13); a disabled child's wrapper is the tab stop with the reason as its name; `inline` makes the trigger an inline box for a control in running text (a read-aloud flag, [ADR 0118](../adr/0118-read-aloud-flags-are-highlight-kinds-with-an-inline-hint-and-skips-and-restarts-show-by-default.md)). `Tooltip` is the info icon: a real button whose text is also its `aria-description`, with a popover on hover, keyboard focus and press; `label` names it when a page has more than one, and Escape closes it wherever focus is (also for a note opened by hover inside a dialog) |
| `Button` | Tokenized button, `primary`/`ghost`/`danger` variants | Bakes in `disabled:pointer-events-none` so future buttons don't need to remember to guard hover-while-disabled by hand. `pending` (ADR 0075): the action it started is running, so it shows a spinner, is `aria-busy` and dimmed, ignores a press and stays focusable (it is not `disabled`, so a keyboard user keeps their place) |
| `IconButton` | Square 32 px button that holds one icon | Required `label` (the accessible name); `default`, `primary` and `danger` variants; a disabled one is dimmed and takes no hover; `pending` swaps the icon for a spinner, marks it `aria-busy` and ignores a press ([ADR 0075](../adr/0075-every-action-that-leaves-the-interface-acknowledges-within-100-ms-cannot-be-fired-twice-and-tells-the-narrator-when-it-ends.md)); a hint is composed around it, `<TooltipTarget text="..."><IconButton/></TooltipTarget>`; `disabledReason` (text) gates it while keeping it focusable and hoverable, unlike native `disabled` - pair it with `<TooltipTarget text={disabledReason}>` so the reason is reachable on the button itself, not a wrapper (D6, WCAG 1.4.13, story bible entries phase 3); it forwards attributes and the ref, so a library part can render as it; `display` has no specificity so `hidden max-md:inline-flex` works ([ADR 0053](../adr/0053-icon-buttons-text-fields-and-selects-wrap-the-native-controls.md)) |
| `TextField` | Bare text input, full width of its container | Named by `label` (aria-label) or an `id` a visible `<label htmlFor>` points at; controlled, `onChange(text)`; `type` `text` or `color`, `mono`; passes attributes and the ref through (the alias combobox builds on it); a control with a visible label, hint or error is a `Field` ([ADR 0053](../adr/0053-icon-buttons-text-fields-and-selects-wrap-the-native-controls.md)) |
| `SearchField` | `TextField` with a clear button inside once there is text | The clear button reports `''` and puts focus back in the field; used by the manuscript reader and the Story Bible list |
| `Select` | The native `<select>`, drawn with the tokens | Stays native on purpose: `fireEvent.change` and Playwright's `selectOption` work; `options` are `{ value, label }`; `onChange(value)`; named like `TextField`; `fullWidth` for a form column |
| `Tabs` (with `TabList`, `Tab`, `TabPanel`) | Tab strip and the content of the selected tab (Base UI Tabs) | Roles, `aria-selected`, panel links and arrow keys from the library; `TabList` needs a `label`; `activation` `manual` (arrows move focus, Enter selects; default) or `automatic`; `underline` or `sidebar` (vertical) look; the strip and panel sit under one `Tabs` root, which may be `contents`; a panel is not a tab stop ([ADR 0054](../adr/0054-tabs-and-toggle-groups-name-and-link-what-the-hand-built-strips-left-anonymous.md)) |
| `ToggleGroup` | A named group of exclusive `Pill`s | `role="group"` with a required `label`; `options` `{ value, label, disabled?, title? }`, `onChange(value)`; every chip stays a tab stop; layout through `className` (`gap-1.5 flex-wrap`) |
| `TagInput` | A list of terms the user keeps: chips, suggested chips, a typing box | One named `role="group"`; owns the draft only and reports `onAdd(text)`, `onRemove(tag)`, `onAcceptSuggestion(term)`; a blank draft reports nothing; focus returns to the box after a removal; an `actions` slot beside Add; used by the Proofing vocabulary hints ([ADR 0055](../adr/0055-the-tag-input-is-a-wrapped-text-field-and-chips-that-report-and-never-edit-the-list.md)) |
| `Table` (with `TableHead`, `TableBody`, `TableRow`, `TableHeader`, `TableCell`) | Presentational data table on the browser's own table elements | `label` names it; `TableRow onActivate` makes a row a control (tab stop, Enter or Space, `selected` sets `aria-selected`, a control inside it keeps its own press, `data-row`); `TableHeader align="right"` for numeric columns and `sorted`/`onSort` for a sortable column (`aria-sort`); `TableCell` is top-aligned; `TableHead sticky`; the caller owns sorting and selection ([ADR 0056](../adr/0056-a-presentational-table-primitive-replaces-the-dtable-class-and-rows-take-the-keyboard.md)) |
| `Highlight` | Highlighted text (entity / note / review, plus the Teleprompter's `Cursor` word and the read-aloud flags) | One category→color mapping; fills the line height; `Cursor` is a solid accent fill, not a tint; the flag kinds `Misread`, `Skipped`, `Restart` (wavy, dotted, dashed underline) and `Extra` (an insertion bar) are decorations only, no tint or text colour; `label` and `description` name and describe an activatable mark ([ADR 0016](../adr/0016-highlight-primitive.md), [ADR 0024](../adr/0024-teleprompter-highlight-follows-the-sidecars-spans.md), [ADR 0118](../adr/0118-read-aloud-flags-are-highlight-kinds-with-an-inline-hint-and-skips-and-restarts-show-by-default.md)) |
| `SlideOver` | Right-edge modal panel with click-away backdrop (Base UI Drawer) | Pure Tailwind ([ADR 0017](../adr/0017-no-legacy-css-shadowing-tailwind.md)); a closed panel is not in the page; Escape, backdrop and Close button close it, Tab loops inside, focus returns to the opener; `data-slide-over` and `data-slide-over-backdrop` mark it ([ADR 0051](../adr/0051-the-slide-over-and-the-navigation-drawer-are-modal-base-ui-drawers.md)) |
| `NavDrawer` | The narrow layout's navigation, a modal drawer from the left | Used only by `AppShell`; same modal behaviour as `SlideOver`; no phone viewport is captured ([ADR 0037](../adr/0037-visual-suite-captures-no-phone-viewport.md)), so its stories are its visual record ([ADR 0051](../adr/0051-the-slide-over-and-the-navigation-drawer-are-modal-base-ui-drawers.md)) |
| `Pill` | Toggle chip (Base UI Toggle: `aria-pressed`) | Active/inactive classes are mutually exclusive so text stays readable in dark mode |
| `Menu` | Menu button and popup of items (Base UI Menu) | Arrow keys, typeahead, Escape, focus back on the button; used for the Story Bible category badge and its Generate/Replace pronunciation control; popup at `z-55`; `render={<IconButton label="..." />}` renders the trigger as another component (an icon-only trigger) instead of pasting the icon-button look, the same pattern as `TooltipTarget` ([ADR 0052](../adr/0052-toggle-menu-checkbox-collapsible-and-switch-replace-the-hand-rolled-widgets.md)) |
| `Popover` | An interactive popup over other content (device pickers, a settings panel), on Base UI Popover | A click opens it, focus moves in and returns to the trigger, Escape closes it and defers to the dialog under it; `trigger` renders the trigger as another component, the same pattern as `Menu`; popup at `z-70`, above a `Dialog`'s `z-60` and below a hint's `z-1000`, so it draws correctly when opened from inside a full-size dialog. Unlike the read-only hint behind the info icon (`Tooltip`'s `Popover`, which never moves focus in), this is for a popup a person operates ([ADR 0172](../adr/0172-popover-is-an-interactive-layer-above-the-dialog-and-escape-defers-to-the-topmost-one.md)) |
| `Checkbox`, `Switch` | Labelled check box and on/off switch rows | Token-styled Base UI controls; pressing the label toggles; `Switch` is the Settings `bool` kind: a row that is the switch, its hint icon and Reset, storing the string "true" or "false" ([ADR 0052](../adr/0052-toggle-menu-checkbox-collapsible-and-switch-replace-the-hand-rolled-widgets.md)) |
| `Collapsible` (with `CollapsibleTrigger`, `CollapsiblePanel`) | Disclosure: button, and a panel that is not in the page while collapsed | `aria-expanded` and `aria-controls` from the library; the button and panel may sit apart inside one section ([ADR 0052](../adr/0052-toggle-menu-checkbox-collapsible-and-switch-replace-the-hand-rolled-widgets.md)) |
| `Disclosure` | A titled group that opens and closes: the whole title row is the button, with a summary that is part of its name | Controlled (`open`, `onOpenChange`); `aside` sits beside the button, never inside it, for an info icon; panel not in the page while collapsed; used for the groups of the import review ([ADR 0086](../adr/0086-the-import-review-is-grouped-by-what-each-section-will-be-and-reports-repairs-instead-of-a-log.md), [import review](../architecture/import-review.md)) |
| `MeterBar` | Segmented horizontal meter | Caller controls segment order — see [ADR 0006](../adr/0006-chapter-progress-bar-ordering.md) for why the chapter-progress usage reverses it. Required `label`: it is an image named by the label plus every segment's tooltip text, and the width transition is `motion-safe:` only ([ADR 0050](../adr/0050-the-segmented-meter-is-an-image-named-by-its-segments-and-field-wires-its-hint-and-error.md)) |
| `ProgressBar` | One job's progress: a bar with a value, or sliding when nothing is measured yet | Base UI `Progress` (`aria-valuenow` only when determinate). Required `label`; `valueText` is what a screen reader hears instead of the number (the bytes of a download), so the numbers stay out of live regions; `running` keeps the fill at least 4% wide; the fill eases and slides only under `motion-safe:`. `WorkDialog` and the Local assets rows draw it ([ADR 0015](../adr/0015-real-progress-only.md)) |
| `Heading` | The page title, with an optional muted line under it | `level` of 1, 2 or 3 (default 1) changes the tag only, the look is the same; the subtitle wraps anywhere, so a file name cannot push the page sideways ([ADR 0058](../adr/0058-heading-has-a-level-and-panel-names-its-region-with-a-level-2-title.md)) |
| `Panel` | Bordered surface around a group of content | With a `title` it is a named region: the title is a level-2 heading and labels the section, and `actions` (only with a title) sit beside it; without one it is the bare surface. Use it for a section a screen reader should be able to jump to ([ADR 0058](../adr/0058-heading-has-a-level-and-panel-names-its-region-with-a-level-2-title.md)) |
| `Field` | Labelled text control (input or textarea) on Base UI Field | Optional `placeholder`, `autoFocus`, `hint` and `error`: the library wires the label, the hint and the error to the control and sets `aria-invalid` ([ADR 0050](../adr/0050-the-segmented-meter-is-an-image-named-by-its-segments-and-field-wires-its-hint-and-error.md)) |
| `TitleSubtitle` | A chapter's (or a book's) title and subtitle, drawn one way | Title semibold, subtitle `--text-muted` and regular, both in source casing (`normal-case`, so an uppercase context cannot re-case them); `layout="inline"` is one run joined by an em dash, `layout="stacked"` puts the subtitle on its own line with a visually hidden dash so both layouts give the same accessible name; `truncate` clips instead of wrapping and shows the full name in a tooltip. Pairs with `apps/ui/src/chapterName.ts`, which formats the same rule as plain text (chapter-title-display-consistency.prd.md) |
| `StatTile` | One KPI figure: an eyebrow label, a mono value, and an optional hint line | `unit` sits beside the value in the label's muted style, not baked into the value string; `progress` (0-1) draws a `ProgressBar` underneath, named after the tile's own `label`; `tone` (`neutral`, `info`, `success`, `warning`, `danger`, `experimental`) tints the value with the matching `-text` token. Promoted from the local copy in `home/RecordingCheckReport.tsx` (studio-ui-primitives.prd.md Phase 4) |
| `Kbd` | Key cap for a hotkey hint (`keys: string[]` joined by "+") | An image named by its keys joined with " + " (like `MeterBar`, ADR 0050); the optional `label` overrides that name for a symbol key (⌫) whose glyph alone would read as a stray character. No platform logic: the input registry (ADR 0361) resolves `Mod` and hands this the caps to draw (studio-ui-primitives.prd.md Phase 2) |
| `StatusBadge` | A closed set of status meanings (`chip` or `dot`), not one tone per stage name | `tone` is `neutral`, `info`, `progress`, `success`, `warning`, `danger` or `experimental` (studio-ui-primitives.prd.md Phase 3, [ADR 0360](../adr/0360-studio-primitives-land-as-flat-leaf-files-after-one-token-batch-and-capability-gating-is-a-primitive.md) Q6); `chip` fills a tint of the tone (Phase 1's `--badge-*-fill` tokens, or an already-declared pair for `neutral`/`progress`, which Phase 1 gave no fill of their own) with the tone's `-text` colour; `dot` is a mark named by `label` (`role="img"`) for a dense list, with no visible text. `label` is the accessible name either way; `icon` is decorative and chip-only. `src/chapterStatus.ts`'s `STATUS_TONE` maps the five chapter statuses onto it (the hand-drawn status dots keep `STATUS_COLOR` for now) |

### Native controls stay in the primitives

A page builds a control from a primitive and never writes the native element ([ADR 0053](../adr/0053-icon-buttons-text-fields-and-selects-wrap-the-native-controls.md)). `apps/ui/src/rawNatives.test.ts` counts the native `button`, `select`, `input`, `textarea` and `table` elements written outside `components/primitives/` against a per-file ceiling that may only go down (`select`, `input`, `textarea` and the table parts have none left), and fails on the pasted icon-button look (`size-8` with `rounded-md`). Use `IconButton` for an icon-only button, `Button` for a text button, `TextField`, `SearchField` or `Field` for a text control, `Select` for a choice, and `Table` for tabular data.

### Import rules

Three import facts are checked, not reviewed ([ADR 0062](../adr/0062-ui-import-rules-are-a-dependency-cruiser-config-and-a-mark-scan-that-name-their-adr.md), `pnpm --dir apps/ui architecture`): a file in `components/primitives/` (a story or a test too) imports nothing else under `components/`, so shared data such as the chapter status labels sits above the feature folders (`src/chapterStatus.ts`); only `src/api/` imports the generated `wailsjs/` bindings and the Wails runtime (`@wailsio/runtime`, [ADR 0200](../adr/0200-the-desktop-shell-runs-on-wails-v3-beta-pinned-at-v3-0-0-beta-25.md)); and only a primitive imports Base UI. `Highlight` is the only file that writes a `<mark>`, with one reasoned exception, the proofing diff's `InlineDiffRow`, which waits on the owner ([ADR 0063](../adr/0063-the-proofing-diff-marks-its-own-words-and-adr-0016-covers-entry-highlights.md), Proposed); `src/highlightBoundary.test.ts` is the check.

### Base UI wrappers

Interactive behaviour (focus trap, Escape, focus return, id wiring, roving focus) comes from Base UI (`@base-ui/react`), and only the primitives import it ([ADR 0047](../adr/0047-the-ui-primitives-wrap-base-ui-and-app-code-never-imports-it.md)). To build a widget:

- **One flat file per primitive** in `primitives/`, with a `<Name>.stories.tsx` and a `<Name>.test.tsx` next to it (a multi-part primitive, `Tabs`, `Table`, `Collapsible`, keeps its parts in one file). A primitive that is only the browser's own element with our tokens (`Select`, `Table`) uses no library part. Import per component (`@base-ui/react/dialog`), never the package root.
- **A closed API.** Keep the props call sites already use, add only explicit new ones, never spread Base UI props and never export a Base UI type. `className` and standard DOM attributes are fine. A part that renders a button is composed with our `Button` through `render`.
- **Style with Tailwind and tokens only:** `var(--token)` values and data-attribute variants on the parts (`data-open:`, `data-starting-style:`, `data-disabled:`, `data-invalid:`), mutually exclusive states ([ADR 0017](../adr/0017-no-legacy-css-shadowing-tailwind.md)), no new class in `styles.css`, none of the library's example CSS.
- **Stacking.** `#root` and the Storybook decorator are `isolate`, so a popup portalled to `<body>` is outside the app's stacking context and always above it. The numeric order among popups is slide-over 50, dialog 60, selection menu 65, nav drawer 70, tooltip and popover 1000.
- **Prove behaviour in a story.** A keyboard behaviour (Escape, Tab trap, focus return) is a `play()` in the wrapper's story, checked in the Chromium atlas; `stories.test.tsx` runs the same story in jsdom, which needs no polyfill for Base UI. Stories find portalled popups on `document.body`, not inside the story canvas.
- **The boundary is a test.** `src/baseUiBoundary.test.ts` fails on any `@base-ui/*` import outside `primitives/`, so a page that needs a new behaviour gets a new primitive first.
- **Bundle budget.** The production JS bundle (`apps/ui/dist/assets/index-*.js`) was 571,969 bytes raw (158,239 gzip) before Base UI, 787,619 raw (231,679 gzip) with every wrapper of the foundation stack, and is 794,295 raw (237,296 gzip) with the native-control, tab, toggle-group, tag-input and table primitives (CSS 44,244): +38.9% over the start. The budget is **800,000 bytes raw**, a documented number and not a CI gate: a change that takes the bundle past it (a Base UI minor bump, a new family) says why in its pull request. Measure with `pnpm --dir apps/ui build` and look at `dist/assets`. It is a desktop app served from disk, so the cost is parse time, not download.
- **What is not wrapped.** The alias combobox in `GuideDetail.tsx` (domain matching and an active index; its input is a `TextField`), `Button`, `NavButton`, `Heading`, `Panel`, `Highlight` and `ErrorBoundary` are plain components with no library part; a page that needs a new widget gets a new primitive.

The custom-CSS system (`.btn`, `.panel-head`/`.panel-body`, `.progressbar`, etc.) that ADR 0003 deliberately left in place for not-yet-migrated consumers has since been fully migrated to Tailwind utilities — see [ADR 0009](../adr/0009-complete-tailwind-migration.md), which supersedes ADR 0003. New UI work should reach for Tailwind utilities directly rather than adding to `styles.css`. ADR 0009 also lists the small set of deliberate exceptions still in `styles.css` (scrollbar-hiding rules, keyframe animations, and a handful of unstyled "marker" classes kept only because visual/unit tests select by CSS class; the generic `table.dtable` style was the fourth and is gone, replaced by `Table`, [ADR 0056](../adr/0056-a-presentational-table-primitive-replaces-the-dtable-class-and-rows-take-the-keyboard.md)).

## Conventions

- **Numeric table columns are right-aligned** (`align="right"` on both the `TableHeader` and its `TableCell`s) — see `AudiobookEstimatePanel.tsx`'s Words/Est./Actual columns for the reference implementation.
- **Disabled interactive elements never show a hover affordance.** Use `disabled:pointer-events-none` (Tailwind) alongside `disabled:opacity-*`, not just the opacity change alone — a hover transform/background change that still fires on a disabled element reads as clickable when it isn't.
- **A field that is read-only, locked or not-yet-persisted is `disabled`, not hidden**, so the user can see what exists without being able to edit it. Story Bible entries open read-only and gain Edit/Save/Cancel controls only on request ([ADR 0018](../adr/0018-story-bible-entries-read-only-until-edit.md)); see `GuideDetail.tsx`.s `editingDisabled`.
- **A non-destructive "peek at something else" action is a `SlideOver` primitive, never a navigation that replaces the current view's state.** See `Manuscript.tsx`'s Chapters & Search overlay and `GuideDetail.tsx`'s "Review entry" overlay — both exist specifically so switching context doesn't discard an in-progress edit.

## Layout and scrolling

The document (`html`/`body`) never scrolls; it is locked (`overflow: hidden`, `styles.css`) once nothing needs it to (app-shell-vertical-overflow.prd.md). Every page scrolls inside the shell's own page area (`AppShell.tsx`'s `<main>`, `.scroll-chrome-hidden`), which is the shell's single scroll container and also gives absolutely positioned descendants (an `sr-only` label, a Base UI hidden input) a containing block so they scroll and clip with their row instead of escaping to the window. Before a project is attached, `StartupScreen` and `ProjectPicker` scroll in the same way inside their own `flex-1 overflow-y-auto` container, with `DemoBanner` above it in a `flex h-full flex-col` so the banner's own height never pushes the document past the viewport. The shell is sized off a `height: 100%` chain from `html`/`body`/`#root` (`h-full`), not `100vh`/`100dvh`, because only the `%` chain stays exact under both WebView2's zoom and a CSS `zoom` on `<html>`.

## Settings rows

`apps/ui/src/components/settings/ScopedSetting.tsx` renders one Settings field by its `kind`, and the host (`apps/desktop/app.go`, `fieldSchemas` and `validateSettingValue`) validates it by the same kind. Every value is a string in the settings files.

| Kind | Control | Stored value |
| --- | --- | --- |
| `choice` | `Select`, full width of its column | one of the schema's choices |
| `text` | `TextField` | any string |
| `color` | a colour swatch and a mono hex `TextField` on one line | six hex digits, no `#`, capitals |
| `bool` | `Switch`: the row is the switch (named by the setting), its hint icon and Reset, not a label column and a control column | `"true"` or `"false"`; anything else reads as off and the host rejects it |

**Layout.** Below `md` (768 px) the label stacks above the control; from `md` to `lg` the label column is a fixed 12rem, from `lg` `minmax(12rem,16rem)`. A grid gives a fixed-range track its full maximum before a flexible one gets any, so a two-column template with no breakpoint left a 45 px control at a 390 px window (defect 8, [ADR 0060](../adr/0060-the-visual-suite-fails-a-collapsed-control-and-a-row-may-declare-one-narrow-on-purpose.md)). Selects and text boxes take the free width (`flex-[1_1_10rem]`, `min-w-0`) up to `max-w-md`, so every select is the same width; the hex box has a 4.5rem minimum beside its swatch; the control row wraps, so Reset shares the line and drops under the control only when there is no room (it does at 768 px). Tailwind utilities on tokens only; no primitive or `styles.css` change.

**Saving.** The form holds every field of the category, and a field with no value in the scope is an empty string, which the host rejects for a choice or a colour. Save sends only the fields whose value changed (`changedValues` in `Settings.tsx`); putting a field with no value of its own back to the value it inherits is no change, and a save with nothing changed does not call the host.

**Adding a field.** Add it to `fieldSchemas` (Go) and give it a default in `config/defaults.json` and in `builtinDefaults` (`internal/settings/store.go`; a test keeps them equal) and in `mockFixtures.ts`. It needs no visual-suite work unless it is a new category, which is a new `settings` catalog row with `...REFLOW`. The suite then checks the row at 390 px: a text box or select narrower than 64 px fails, and the PNG at `screenshots/app/settings/<state>/reflow.png` is one to look at with the other viewports ([ADR 0061](../adr/0061-settings-states-are-also-captured-at-a-390px-reflow-width.md)). The first `bool` field also needs its own state so the Switch row is captured; none ships yet (the notification and Build Story Bible after import settings, owner decision D8, are the first).

**Local assets.** The Local assets category is a list, not field rows: one `li` per asset (`apps/ui/src/components/assets/LocalAssetRow.tsx`) in the `surface-2` block look of the other Settings panels, with the asset name as a level-3 heading under the category's level-2, its facts as a definition list that wraps, a `ProgressBar` while it downloads, one live region (`role="status"`) for progress and results, a failure as `role="alert"` in the row, and its actions in a wrapping row at the bottom so nothing collapses at 390 px. Its buttons are named with the kind and the asset ("Verify Whisper model Small"); the one action slot keeps its place as the state changes (Download, then Cancel, then a pending Verifying, then Verify) so keyboard focus is not dropped. It has no text box or select, so the collapsed-control check has nothing to measure; it is still captured at the reflow width like every Settings state.

**Known at 390 px, outside Settings** (found by a one-off sweep of every state at that width; the collapsed-control check passes everywhere): the Proofing results header and table (#148), the Home chapter table (#149) and a three-action confirm dialog (#150) are clipped or cramped. They need layout decisions and are reachable only by zoom, since the shell's minimum window is 960 px.

## Manuscript reader

- **One shared card.** A chapter row and a credits row (Opening/Closing credits) are both `ReaderCard` (`apps/ui/src/components/manuscript/ReaderCard.tsx`, not a primitive: it is used only on the Manuscript page). The whole header is the disclosure: a native toggle `<button>` whose `::after` is stretched over the entire header (`after:absolute after:inset-0`, no `position:relative` on the button itself, so it escapes to the header's own `sticky` positioning context), with `aria-expanded`/`aria-controls`. The bookmark and Read aloud controls sit `relative z-[1]` above that overlay so they keep their own presses; a card with no bookmark (credits, MC1) still renders an empty leading grid cell rather than omitting it — an *absent* React child is not a grid item, which shifts every later column and collapses the last one (manuscript-credits-card-parity.prd.md; manuscript-chapter-header-alignment.prd.md; ADR 0190).
- **The header's right side is one fixed order:** an optional Retail sample tag, then the stat block (word count, read time), then a fixed-width action slot (Read aloud, or empty on a row with no action, so the slot still lines up), then a chevron, in that order, so the stats and the action column line up down the page regardless of how many digits a count has (manuscript-chapter-header-alignment.prd.md, ADR 0190). The stat block is `min-width`, not `width` (a very long count grows just that row rather than overflowing); the action slot is a fixed width with the button right-aligned inside it, so a row with no action still reserves the same space.
- **Read time** (`readTimeLabel` in `apps/ui/src/state.ts`) is shared by every card: seconds under a minute (`~2 s read`), minutes at and above it (`~16 min read`) — the same 200 words/minute figure a chapter always used, just no longer rounded up to "~1 min read" for a short credits segment.
- **Credits follow the reader**, not a fixed look of their own: Text size, sticky header, one row per line (no source line numbers — those are for "Go to line"/search, which credits have none of), and the chevron/whole-header toggle every card has. Their open state is not host reader state (a credits id is never `expandedChapters`, a bookmark or `manuscriptParagraphs`); it is a per-project browser-storage preference, open by default (`creditsExpandedStorage.ts`, MC5 b).
- **Alternating rows.** Paragraph rows alternate between `--surface` and `--row-alt` (a touch darker in light theme, a touch lighter in dark) with a 1px `--border` line between rows, like banded table rows. Highlights tint with `transparent` mixes so they read on either row.
- **Highlights** are the `Highlight` primitive only ([ADR 0016](../adr/0016-highlight-primitive.md)): entity, note and review share one look, filling the full line height, tinted and underlined in the kind's color and drawn in its derived `--<kind>-text` ([ADR 0059](../adr/0059-text-colours-meet-wcag-aa-with-two-text-levels-a-non-text-token-and-derived-on-tint-text.md)).
- **Sticky chapter headers** are opaque (`bg-[var(--surface)]`) with a soft shadow once stuck; they must never be transparent over scrolling text.
- **"Go to line" target** stays highlighted (accent edge and tint) for 30 seconds (`JUMP_HIGHLIGHT_MS` in `Manuscript.tsx`), with a brief ring pulse on arrival.
- **Formatting and line breaks** from import (`spans`, `\n`) render as `<strong>`/`<em>`/`<u>` and `white-space: pre-line` ([ADR 0014](../adr/0014-inline-formatting-as-offset-spans.md)).
- **Side panels** use `SlideOver`; state-dependent classes are mutually exclusive ([ADR 0017](../adr/0017-no-legacy-css-shadowing-tailwind.md)).

## Chapter names

A chapter's name (its title and, when it has one, its subtitle) is drawn one way everywhere: **`Title — Subtitle`**, an em dash (space, U+2014, space), the title semibold and the subtitle `--text-muted` and regular, both in source casing — never CSS capitals, never italic, never monospace (ADR 0191). A context prefix ("Read aloud", "Recording check", "Stage suggestion") uses `: ` instead, so the em dash only ever means "a subtitle follows" (`Read aloud: PROLOGUE — The Last Good Applause`, never `Read aloud — PROLOGUE — …`, ADR 0261). `apps/ui/src/chapterName.ts` formats it as plain text (`full`, `short`, or `context(prefix)`); `TitleSubtitle` (above) draws it as markup, and the read-aloud reading heading (`ReaderText.tsx`) draws the same `stacked` convention by hand, since its title is per-word tracked markup, not a plain string - the subtitle sits under it, in view but not tracked (Q9). A source-scan guard (`apps/ui/src/chapterNameFormatting.test.ts`, in the style of `rawNatives.test.ts`) ratchets down every other read of `.subtitle` inside JSX or a template literal under `src/components/**`; a call site that still needs to show the second line on its own (not as part of a formatted name — a checkbox naming which line it is about, a search key) is not what the guard is for and stays off it. Migrated: the Manuscript chapter and credits card headers (`ReaderCard`), the Chapters & Search rows (`ChapterNav`), the import review's section rows (`ImportReview`), the Home chapter table (`AudiobookEstimatePanel`), the read-aloud dialog title and reading heading, the standalone Teleprompter's chapter select, and the Recording check and Stage suggestion dialog titles. Every other title-only site (a toast, a Tracks row, a Story Bible evidence row) still reads the bare title directly, which is already what `chapterName`'s `short` form would give it - no visible difference, so routing them through the formatter is follow-up, not a defect. The host and the sidecars follow the same rule through `apps/desktop/internal/chaptername` and `narration_common.chapter_names` (with a plain form, `Title - Subtitle`, for outputs that cannot take an em dash), pinned to `chapterName.ts` by `tests/fixtures/chapter-names.json` ([ADR 0239](../adr/0239-host-and-sidecar-chapter-names-follow-the-display-rule-through-one-helper-per-language-and-matching-keys-keep-their-own-form.md)).

## CSS layering gotcha

`styles.css` wraps only *some* rules in `@layer base`/`@layer components` (roughly lines 66–616); everything after that is unlayered plain CSS. Unlayered rules always beat Tailwind utilities (which live in the implicit `@layer utilities`) for the same property, regardless of source order. Before adding a Tailwind override next to a legacy class name, check whether that class is inside a `@layer` block — if it's unlayered, the Tailwind utility will silently do nothing for any property the legacy rule already sets.

## Motion

See [`motion-and-animation.md`](motion-and-animation.md) — not yet a formal system. Motion is a `motion-safe:` utility (`MeterBar`'s segment transition, `WorkDialog`'s fill), and `components.css` declares none (`apps/ui/src/legacyCss.test.ts`), because a legacy rule beats a utility in the same layer ([ADR 0057](../adr/0057-a-running-job-that-cannot-be-cancelled-keeps-its-dialog-blocking-and-says-so.md)).

## What the suites do not prove

The visual suite is a gate on errors, not on appearance: it fails on page errors, failed requests, sideways overflow, a collapsed control (a text box or select under 64 px, [ADR 0060](../adr/0060-the-visual-suite-fails-a-collapsed-control-and-a-row-may-declare-one-narrow-on-purpose.md); the Settings rows are also captured at a 390 px reflow width, [ADR 0061](../adr/0061-settings-states-are-also-captured-at-a-390px-reflow-width.md)), blank screenshots and two states that render identically without a declared `sameAs`, and it says nothing about whether a layout looks right ([ADR 0023](../adr/0023-visual-suite-capture-contract-and-storybook.md)). Axe runs on Storybook stories in the atlas (`apps/ui/tests/atlas/atlas.spec.ts`) and on every app state in the visual suite: a violation fails the capture unless `apps/ui/tests/visual/axe-debt.ts` declares it with a reason, and a declared rule that stops being reported fails too, so the list only shrinks ([ADR 0064](../adr/0064-the-visual-suite-runs-axe-on-every-app-state-and-a-violation-fails-unless-it-is-declared-debt.md); `UI_AXE=1` measures without failing). Aria snapshots pin the role trees of the dialogs, the slide-over and the navigation in a run of their own (`apps/ui/tests/aria`, [ADR 0065](../adr/0065-aria-snapshots-pin-the-role-trees-of-the-dialogs-the-slide-over-and-the-navigation.md)). None of this can judge gradients or low-contrast text over semi-transparent overlays, focus order or what a screen reader announces, so contrast by eye and a screen-reader pass remain. Pixel baselines are not adopted: the captures on one machine are now byte-identical from run to run (282 of 282 PNGs across repeated and CPU-throttled runs on the same Windows machine), which does not reproduce the earlier measurement (9 of 244 differing between two runs, [ADR 0023](../adr/0023-visual-suite-capture-contract-and-storybook.md)); but all of those runs were on one machine, nothing here shows that two machines or operating systems agree, and a baseline would have to come from a pinned Linux container. The spike that would decide this is [#153](https://github.com/countrymanprime/narration-utils/issues/153), after the suite has been stable for 50 runs, and its result is recorded as an ADR either way. The suite's own reliability rules (waits are conditions, `UI_CPU_THROTTLE` to prove a driver) are in [CI and releases](../operations/ci-and-releases.md#keeping-a-red-run-meaningful).

Defects these suites have found that are not fixed yet, and the planned work to close the gaps above, are specified as PRDs in [docs/prds/](../prds/README.md).
